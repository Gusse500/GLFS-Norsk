<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY shadow-root "https://github.com/shadow-maint/shadow/releases/download">
  <!ENTITY shadow-download "&shadow-root;/&shadow-version;/shadow-&shadow-version;.tar.xz">
]>

<sect1 id="shadow" xreflabel="Shadow-&shadow-version;">
  <?dbhtml filename="shadow.html"?>


  <title>Shadow-&shadow-version;</title>

  <indexterm zone="shadow">
    <primary sortas="a-Shadow">Shadow</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til Shadow</title>

    <para>
      Shadow ble faktisk installert i LFS, og det er ingen 
      grunn til å installere det på nytt med mindre du 
      installerte Linux-PAM etter at LFS-systemet ditt var 
      ferdig. Hvis du har installert Linux-PAM, vil det å 
      installere Shadow på nytt tillate programmer som
      <command>login</command> og <command>su</command> å bruke PAM.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&shadow-download;"/>
        </para>
      </listitem>
    </itemizedlist>

<!--
    <bridgehead renderas="sect3">Ytterligere Nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nødvendig oppdatering:
          <ulink url="&patch-root;/shadow-&shadow-version;-useradd_segfault-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->
    <bridgehead renderas="sect3">Shadow Avhengigheter</bridgehead>

    <para role="required">
      <xref linkend="linux-pam"/>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av Shadow</title>

    <important>
      <para>
        Installasjonskommandoene som vises nedenfor 
        er for installasjoner der Linux-PAM er installert 
        og Shadow installeres på nytt for å støtte 
        Linux-PAM installasjonen.
      </para>
    </important>

    <warning>
      <para>
        Hvis du installerer shadow på nytt for en versjonsoppdatering, 
        må du sørge for å fullføre Linux-PAM konfigurasjonen nedenfor på nytt. 
        Installasjonen av shadow overskriver mange av filene i 
        <filename class="directory">/etc/pam.d/</filename>.
      </para>
    </warning>

    <para>
      Først, deaktiver installasjonen av noen manualsider,
      bruk <emphasis>YESCRYPT</emphasis>, endre postboksplasseringer, fjern 
      symbollenkede mapper fra <envar>PATH</envar>, og inkludere riktige 
      deklarasjoner i noen C filer:
    </para>

<screen><userinput remap="pre">find man -name Makefile.in                \
         -exec sed -e 's/getspnam\.3 / /' \
                   -e 's/passwd\.5 / /'   \
                   -i {} \; &amp;&amp;

sed -e 's@#ENCRYPT_METHOD DES@ENCRYPT_METHOD YESCRYPT@' \
    -e 's@/var/spool/mail@/var/mail@'                   \
    -e '/PATH=/{s@/sbin:@@;s@/bin:@@}'                  \
    -i etc/login.defs &amp;&amp;

sed -e '/stdio.h/i #include &lt;stdint.h&gt;' \
    -i lib/find_new_sub_*ids.c</userinput></screen>

    <para>
      Installer Shadow på nytt ved å kjøre følgende 
      kommandoer:
    </para>

<screen><userinput>./configure --sysconfdir=/etc   \
            --disable-static    \
            --without-libbsd    \</userinput><userinput revision="openrc,sysv">
            --disable-logind    \</userinput><userinput>
            --with-{b,yes}crypt &amp;&amp;
make</userinput></screen>

    <para>
      Nå, som &root; bruker:
    </para>

<screen role="root"><userinput>make exec_prefix=/usr pamddir= install</userinput></screen>

    <para>
      Manualsidene ble installert i LFS, men hvis ny installasjon er ønskelig, 
      kjør (som &root; bruker):
    </para>

<screen role="root"><userinput>make -C man install-man</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-configure;

    <para>
      <command>sed -i 's/groups$(EXEEXT) //' src/Makefile.in</command>: Denne 
      sed brukes til å undertrykke installasjonen av <command>groups</command>
      siden programversjonen fra Coreutils pakken som ble 
      installert under LFS er å foretrekke.
    </para>

    <para>
      <command>find man -name Makefile.in -exec ... {} \;</command>: Den første 
      kommandoen brukes til å undertrykke installasjonen av
      <command>groups</command> manualsider slik at de eksisterende som er 
      installert fra Coreutils pakken ikke erstattes. De to andre kommandoene 
      forhindrer installasjon av manualsider som allerede er installert av 
      i LFS.
    </para>

    <para>
      <command>sed -e 's@#ENCRYPT_METHOD DES@ENCRYPT_METHOD YESCRYPT@' -e
        's@/var/spool/mail@/var/mail@' -e '/PATH=/{s@/sbin:@@;s@/bin:@@}'
        -i etc/login.defs</command>: I stedet for å bruke standardmetoden «DES» 
      endrer denne kommandoen installasjonen til å bruke den mye sikrere 
      «YESCRYPT» metoden for hashing av passord, som også tillater passord 
      som er lengre enn åtte tegn. Kommandoen endrer også den foreldede 
      <filename class="directory">/var/spool/mail</filename> location
      for brukerpostbokser som Shadow bruker som standard til 
      <filename class="directory">/var/mail</filename>
      plassering. Den endrer også standardbanen slik at den er i samsvar med 
      den som er angitt i LFS.
    </para>

    <para>
      <parameter>--without-libbsd</parameter>: Hindrer å lete etter
      <command>readpassphrase</command> funksjon, som bare finnes i
      <filename class="libraryfile">libbsd</filename>, som ikke er i GLFS. 
      En intern implementering av <command>readpassphrase</command> brukes 
      i stedet.
    </para>

    <para revision="openrc,sysv">
      <parameter>--disable-logind</parameter>: Dette alternativet er påkrevd 
      for systemer uten <filename class="libraryfile">libsystemd</filename>.
      <xref linkend="elogind"/> tilbyr ikke det biblioteket.
    </para>

    <para>
      <parameter>pamddir=</parameter>: Forhindrer installasjon av de 
      medfølgende PAM konfigurasjonsfilene i
      <filename class='directory'>/etc/pam.d</filename>. Den leverte 
      konfigurasjonen fungerer ikke med GLFS PAM konfigurasjonen, og vi vil 
      opprette disse konfigurasjonsfilene eksplisitt.
    </para>

<!-- This is the default
    <para>
      <parameter>-\-with-group-name-max-length=32</parameter>: Maksimumslengden 
      for brukernavn er 32 tegn. Gjør det maksimale gruppenavnet likt.
    </para>
    -->
<!--
    <para>
      <parameter>-\-without-su</parameter>: Ikke installer på nytt
      <command>su</command> fordi upstream anbefaler å bruke
      <command>su</command> kommandoen fra <xref linkend='util-linux'/>
      når Linux-PAM er tilgjengelig.
    </para>
-->
  </sect2>

<!-- Now, /etc/default/useradd is not reinstalled anymore, and this
     configuration has been done in lfs
  <sect2 role="configuration">
    <title>Configuring Shadow</title>

    <para>
      Shadows standardkonfigurasjon for
      <command>useradd</command> verktøyet er kanskje ikke ønskelig for din 
      installasjon. Én standardparameter forårsaker <command>useradd</command> 
      å opprette en postboksfil for enhver nyopprettet bruker.
      <command>useradd</command> vil gjøre gruppeeierskapet til denne filen til
      <systemitem class="groupname">mail</systemitem> gruppe med 0660 
      tillatelser. Hvis du foretrekker at disse postboksfilene ikke opprettes av 
      <command>useradd</command>, utfør følgende kommando som
      <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>sed -i 's/yes/no/' /etc/default/useradd</userinput></screen>
  </sect2>
-->
  <sect2 role="configuration">
    <title>Konfigurere Linux-PAM til å fungere med Shadow</title>

    <sect3 id="pam.d">
      <title>Konfigurasjonsfiler</title>

      <para>
        <filename>/etc/pam.d/*</filename> eller alternativt
        <filename>/etc/pam.conf</filename>,
        <filename>/etc/login.defs</filename> og
        <filename>/etc/security/*</filename>
      </para>

      <indexterm zone="shadow pam.d">
        <primary sortas="e-etc-pam.d">/etc/pam.d/*</primary>
      </indexterm>

      <indexterm zone="shadow pam.d">
        <primary sortas="e-etc-pam.conf">/etc/pam.conf</primary>
      </indexterm>

      <indexterm zone="shadow pam.d">
        <primary sortas="e-etc-login.defs">/etc/login.defs</primary>
      </indexterm>

      <indexterm zone="shadow pam.d">
        <primary sortas="e-etc-security">/etc/security/*</primary>
      </indexterm>
    </sect3>

    <sect3>
      <title>Konfigurasjonsinformasjon</title>

      <para>
        Å konfigurere systemet ditt til å bruke Linux-PAM kan 
        være en kompleks oppgave. Informasjonen nedenfor gir et 
        grunnleggende oppsett slik at Shadows påloggings og 
        passord funksjonalitet fungerer effektivt med Linux-PAM. 
        Se gjennom informasjonen og lenkene på 
        <xref linkend="linux-pam"/> side for ytterligere 
        konfigurasjonsinformasjon informasjon. For informasjon 
        spesifikk for integrering av Shadow, Linux-PAM og 
        libpwquality, kan du besøke følgende 
        lenke:
      </para>

      <itemizedlist spacing="compact">
        <listitem>
          <!-- Old URL redirects to here. -->
          <para>
            <ulink url="http://web.archive.org/web/20240812102759/https://deer-run.com/users/hal/linux_passwords_pam.html"/>
          </para>
        </listitem>
      </itemizedlist>

      <sect4 id="pam-login-defs">
        <title>Konfigurere /etc/login.defs</title>

        <para>
          <command>login</command> programmet utfører for tiden mange funksjoner 
          som Linux-PAM-moduler nå skal håndtere. Følgende <command>sed</command> 
          kommando vil kommentere ut de aktuelle linjene i 
          <filename>/etc/login.defs</filename>, og stoppe <command>login</command> 
          fra å utføre disse funksjonene (en sikkerhetskopifil med navnet 
          <filename>/etc/login.defs.orig</filename> opprettes også for å bevare 
          innholdet i den opprinnelige filen). Utfør følgende kommandoer 
          som <systemitem class="username">root</systemitem> bruker:
        </para>

        <indexterm zone="shadow pam-login-defs">
          <primary sortas="e-etc-login.defs">/etc/login.defs</primary>
        </indexterm>

<screen role="root"><userinput>&install-file; /etc/login.defs /etc/login.defs.orig &amp;&amp;
for FUNCTION in FAIL_DELAY               \
                FAILLOG_ENAB             \
                LASTLOG_ENAB             \
                MAIL_CHECK_ENAB          \
                OBSCURE_CHECKS_ENAB      \
                PORTTIME_CHECKS_ENAB     \
                QUOTAS_ENAB              \
                CONSOLE MOTD_FILE        \
                FTMP_FILE NOLOGINS_FILE  \
                ENV_HZ PASS_MIN_LEN      \
                SU_WHEEL_ONLY            \
                CRACKLIB_DICTPATH        \
                PASS_CHANGE_TRIES        \
                PASS_ALWAYS_WARN         \
                CHFN_AUTH ENCRYPT_METHOD \
                ENVIRON_FILE
do
    sed -i "s/^${FUNCTION}/# &amp;/" /etc/login.defs
done</userinput></screen>
      </sect4>

      <sect4>
        <title>Konfigurere /etc/pam.d/ Filen</title>

        <para>
          Som nevnt tidligere i Linux-PAM instruksjonene, har Linux-PAM to 
          støttede metoder for konfigurasjon. Kommandoene nedenfor forutsetter 
          at du har valgt å bruke en mappebasert konfigurasjon, der hvert 
          program har sin egen konfigurasjonsfil. Du kan eventuelt bruke en 
          enkelt
          <filename>/etc/pam.conf</filename> konfigurasjonsfil ved å bruke 
          teksten fra filene nedenfor, og oppgi programnavnet som et ekstra 
          første felt for hver linje.
        </para>

        <para>
          Som <systemitem class="username">root</systemitem> bruker, opprett
          følgende Linux-PAM konfigurasjonsfiler
          i <filename class="directory">/etc/pam.d/</filename> mappen
          (eller legg innholdet til <filename>/etc/pam.conf</filename> filen)
          ved å bruke følgende kommandoer:
        </para>
      </sect4>

      <sect4>
        <title>'login'</title>

<screen role="root"><userinput>cat &gt; /etc/pam.d/login &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/login

# Set failure delay before next prompt to 3 seconds
auth      optional    pam_faildelay.so  delay=3000000

# Check to make sure that the user is allowed to login
auth      requisite   pam_nologin.so

# Check to make sure that root is allowed to login
# Disabled by default. You will need to create /etc/securetty
# file for this module to function. See man 5 securetty.
#auth      required    pam_securetty.so

# Additional group memberships - disabled by default
#auth      optional    pam_group.so

# include system auth settings
auth      include     system-auth

# check access for the user
account   required    pam_access.so

# include system account settings
account   include     system-account

# Set default environment variables for the user
session   required    pam_env.so

# Set resource limits for the user
session   required    pam_limits.so

# Display the message of the day - Disabled by default
#session   optional    pam_motd.so

# Check user's mail - Disabled by default
#session   optional    pam_mail.so      standard quiet

# include system session and password settings
session   include     system-session
password  include     system-password

# End /etc/pam.d/login</literal>
EOF</userinput></screen>
      </sect4>

      <sect4>
        <title>'passwd'</title>

<screen role="root"><userinput>cat &gt; /etc/pam.d/passwd &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/passwd

password  include     system-password

# End /etc/pam.d/passwd</literal>
EOF</userinput></screen>
      </sect4>

      <sect4>
        <title>'su'</title>

<screen role="root"><userinput>cat &gt; /etc/pam.d/su &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/su

# always allow root
auth      sufficient  pam_rootok.so

# Allow users in the wheel group to execute su without a password
# disabled by default
#auth      sufficient  pam_wheel.so trust use_uid

# include system auth settings
auth      include     system-auth

# limit su to users in the wheel group
# disabled by default
#auth      required    pam_wheel.so use_uid

# include system account settings
account   include     system-account

# Set default environment variables for the service user
session   required    pam_env.so

# include system session settings
session   include     system-session

# End /etc/pam.d/su</literal>
EOF</userinput></screen>
      </sect4>

      <sect4>
        <title>'chpasswd' og 'newusers'</title>

<screen role="root"><userinput>cat &gt; /etc/pam.d/chpasswd &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/chpasswd

# always allow root
auth      sufficient  pam_rootok.so

# include system auth and account settings
auth      include     system-auth
account   include     system-account
password  include     system-password

# End /etc/pam.d/chpasswd</literal>
EOF

sed -e s/chpasswd/newusers/ /etc/pam.d/chpasswd >/etc/pam.d/newusers</userinput></screen>
      </sect4>

      <sect4>
        <title>'chage'</title>

<screen role="root"><userinput>cat &gt; /etc/pam.d/chage &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/chage

# always allow root
auth      sufficient  pam_rootok.so

# include system auth and account settings
auth      include     system-auth
account   include     system-account

# End /etc/pam.d/chage</literal>
EOF</userinput></screen>
      </sect4>

      <sect4>
        <title>Andre shadow verktøy</title>

<screen role="root"><userinput>for PROGRAM in chfn chgpasswd chsh groupadd groupdel \
               groupmems groupmod useradd userdel usermod
do
    &install-file; /etc/pam.d/chage /etc/pam.d/${PROGRAM}
    sed -i "s/chage/$PROGRAM/" /etc/pam.d/${PROGRAM}
done</userinput></screen>

        <warning>
          <para>
            På dette tidspunktet bør du gjøre en enkel test for å se om 
            Shadow fungerer som forventet. Åpne en annen terminal og logg 
            inn som
            <systemitem class="username">root</systemitem>, og så kjør
            <command>login</command> og logg inn som en annen bruker. 
            Hvis du ikke ser noen feil, er alt i orden, og du bør 
            fortsette med resten av konfigurasjonen. Hvis du fikk feil, 
            stopp nå og dobbeltsjekk konfigurasjonsfilene 
            ovenfor manuelt. Enhver feil er et tegn 
            på en feil i prosedyren ovenfor. 
            Du kan også kjøre testpakken fra Linux-PAM pakken for 
            å hjelpe deg med å finne problemet. Hvis du ikke finner 
            og fikser feilen, bør du kompilere Shadow på nytt ved å 
            legge til
            <option>--without-libpam</option> bryteren til
            <command>configure</command> kommandoen i instruksjonene ovenfor 
            (flytt også <filename>/etc/login.defs.orig</filename> sikkerhetskopi 
            filen til <filename>/etc/login.defs</filename>). Hvis du ikke gjør 
            dette og feilmeldingene vedvarer, vil du ikke kunne logge deg på 
            systemet ditt.
          </para>
        </warning>
      </sect4>

      <sect4 id="pam-access">
        <title>Konfigurere Innloggingstilgang</title>

        <para>
          I stedet for å bruke <filename>/etc/login.access</filename> filen for 
          å kontrollere tilgang til systemet, Linux-PAM bruker 
          <filename class='libraryfile'>pam_access.so</filename> modulen
          sammen med <filename>/etc/security/access.conf</filename> filen.
          Gi nytt navn til <filename>/etc/login.access</filename> filen ved å 
          bruke følgende kommando:
        </para>

        <indexterm zone="shadow pam-access">
          <primary sortas="e-etc-security-access.conf">/etc/security/access.conf</primary>
        </indexterm>
<!-- to editors: it is a common belief that:
        if <condition>; then <command>; fi
     is equivalent to:
        <condition> && <command>
     This is not true in bash; try:
        ([ 0 = 1 ] && echo not reachable); echo $? # echoes 1
     vs
        (if [ 0 = 1 ]; then echo not reachable; fi); echo $? # echoes 0
     So in scripts that may call subshells (for example through sudo) and
     that need error reporting, the outcome _is_ different. In all
     cases, for bash, the "if" form should be preferred.-->
<screen role="root"><userinput>if [ -f /etc/login.access ]; then mv -v /etc/login.access{,.NOUSE}; fi</userinput></screen>
      </sect4>

      <sect4 id="pam-limits">
        <title>Konfigurere ressursgrenser</title>

        <para>
          I stedet for å bruke <filename>/etc/limits</filename> filen for å 
          begrense bruken av systemressurser,
          Linux-PAM bruker
          <filename class='libraryfile'>pam_limits.so</filename> modulen sammen
          med <filename>/etc/security/limits.conf</filename> filen. Gi nytt navn
          til <filename>/etc/limits</filename> filen ved å bruke følgende kommando:
        </para>

        <indexterm zone="shadow pam-limits">
          <primary sortas="e-etc-security-limits.conf">/etc/security/limits.conf</primary>
        </indexterm>

<screen role="root"><userinput>if [ -f /etc/limits ]; then mv -v /etc/limits{,.NOUSE}; fi</userinput></screen>

        <caution>
          <para>
          Sørg for å teste systemets påloggingsmuligheter før du logger ut. 
          Feil i konfigurasjonen kan forårsake permanent utlåsing som krever 
          oppstart fra en ekstern kilde for å rette 
          problemet.
          </para>
        </caution>

      </sect4>
    </sect3>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <para>
      En liste over de installerte filene, sammen med korte beskrivelser, 
      finner du på
      <ulink url="&mlfs-root;/chapter08/shadow.html#contents-shadow"/>.
    </para>

  </sect2>

</sect1>
