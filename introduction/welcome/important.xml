<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="important" xreflabel="Viktig informasjon">
  <?dbhtml filename="important.html"?>

  <title>Viktig informasjon</title>

  <para>
    BLFS har mer informasjon om kompilering, /usr vs /usr/local,
    oppstartsskript osv. på
    <ulink url="&blfs-svn;/introduction/important.html"/>.
    I motsetning til denne boken, ville dette vært et kapittel i BLFS.
    Mye av denne informasjonen er utelatt, ettersom denne boken er mer lineær
    og ikke følger hvordan BLFS følger. Vi vil imidlertid dekke noen grunnleggende
    elementer her.
  </para>

  <sect2 id="which-instructions" xreflabel="Hvilke instruksjoner som skal følges">
    <title>Hvilke instruksjoner som skal følges</title>

    <para>
      Denne boken er forskjellig fra de vanlige LFS og BLFS bøkene, ettersom den også har
      installasjonsinstruksjoner for lib32. Dette vil garantert føre til forvirring.
    </para>

    <para>
      Når du ser <quote>Installasjon av ...</quote>, disse instruksjonene
      bør følges hvis du trenger den pakken. Etter hvert som du går videre, kan du
      komme over et annet format, og boken vil instruere deg om hva du skal
      følge på det tidspunktet. Du bør lese <xref linkend="multilib"/> først,
      og for det andre, må du sannsynligvis gjøre mer enn den vanlige installasjonen.
    </para>

    <para>
      Det er her seksjonene merket
      <quote>lib32 Installasjon av ...</quote> kommer inn. Disse seksjonene gir
      32-bits biblioteker for 32-bits programvarekompatibilitet, dvs. støtte for multilib.
      Hvis du ikke trenger 32-bits bibliotekene fra en gitt pakke, kan du hoppe over
      lib32 installasjonsinstruksjonene for den pakken.
    </para>

  </sect2>

  <sect2>
    <title>Stabilitet</title>

    &important-stability;

  </sect2>

  <sect2>
    <title>Init System</title>

    <para revision="sysv">
      Denne versjonen av boken er ment for SysV LFS-systemer. Systemd
      versjonen av boken kan ikke vises på nett av &copyholder; på grunn av
      begrensninger på Github Pages. Dermed må du laste ned eller gjengi boken
      selv. Følg instruksjonene <ulink url="&glfs-repo;">GLFS rendering
      instructions</ulink> for å kunne gjengi boken selv. Du trenger
      <ulink url='&blfs-svn;/general/git.html'>Git</ulink> å klone
      depotet. Du kan laste ned boken <ulink
      url="&glfs-repo;/releases">her</ulink>.
    </para>

    <para revision="systemd">
      Denne versjonen av denne boken er ment for Systemd LFS systemer.
    </para>

  </sect2>

  <sect2>
    <title>Bygge programvare</title>

    <para>
      Å bygge programvare på GLFS er identisk med hvordan det gjøres i
      BLFS bøkene, i tillegg til at det finnes lib32 kompileringsinstruksjoner. Det
      sier seg selv at du først og fremst bør ha <envar>MAKEFLAGS</envar>
      satt for å spare deg mye tid. Dette er nyttig for <command>make
      </command> verktøyet for å bruke mengden tråder du både ønsker
      og har.
    </para>

<screen><userinput>export MAKEFLAGS='-jx'</userinput></screen>

    <para>
      Erstatt <option>x</option> med antall tråder du har.
      Du kan sjekke antall tråder du har med:
    </para>

<screen><userinput>grep processor /proc/cpuinfo</userinput></screen>

    <important>
      <para>
        Sørg for at du har nok RAM til systemet ditt!
        En generell metode er å ha 2,5 G per tråd som du vil bruke for
        <command>make</command>. Hvis du for eksempel vil bruke 6 tråder,
        multipliser 6 med 2,5 (som er 15), og sørg for at du har 15 GB RAM.
        Hvis du ikke har den mengden RAM, kan du prøve å begrense antall tråder du bruker.
        <command>make</command> eller bruke vekselfil for å oppfylle det kravet.
      </para>
    </important>

    <para>
      Neste steg er kompilering for lib32. Det finnes mange pakker som vil ha
      en lib32 motpart. Hvis du nettopp er ferdig med en vanlig kompilering
      av en pakke og ønsker å gjøre en lib32 kompilering av den samme pakken,
      sørg for å rense mappen først:
    </para>

<screen><userinput>make distclean</userinput></screen>

    <para>Eller, hvis du har laget en byggemappe, gjør dette i
    byggemappeen:</para>

    <screen><userinput>rm -rf *</userinput></screen>

    <para>Fortsett deretter med lib32 kompileringsinstruksjonene.</para>

    <para>
      Vanligvis er formatet for målretting av lib32 slik:
    </para>

    <para>For <command>./configure</command>:</para>

<screen><userinput>CC="gcc -m32" CXX="g++ -m32"                  \
PKG_CONFIG_PATH=/usr/lib32/pkgconfig          \
./configure --prefix=/usr --libdir=/usr/lib32 \
            --host=i686-pc-linux-gnu &amp;&amp;

make &amp;&amp;

make DESTDIR=$PWD/DESTDIR install     &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

    <para>For <command>meson</command>:</para>

<screen><userinput>mkdir -v build &amp;&amp;
cd       build &amp;&amp;

CC="gcc -m32" CXX="g++ -m32"                     \
PKG_CONFIG_PATH=/usr/lib32/pkgconfig             \
meson setup .. --prefix=/usr --libdir=/usr/lib32 &amp;&amp;

ninja &amp;&amp;

DESTDIR=$PWD/DESTDIR ninja install    &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

    <para>For <command>cmake</command>:</para>

<screen><userinput>mkdir -v build &amp;&amp;
cd       build &amp;&amp;

CC="gcc -m32" CXX="g++ -m32"           \
PKG_CONFIG_PATH="/usr/lib32/pkgconfig" \
cmake .. -DCMAKE_INSTALL_PREFIX=/usr   \
         -DCMAKE_INSTALL_LIBDIR=lib32 &amp;&amp;

make &amp;&amp;

make DESTDIR=$PWD/DESTDIR install     &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

    <note>
      <para>
        Etter at du har utført en DESTDIR installasjon, anbefales det
        å bruke <command>file</command> på et av bibliotekene i
        <filename>DESTDIR/usr/lib32</filename>. Utdata fra en slik
        kommando for en lib32 bygging av et bibliotek bør være
        sammenlignbar med følgende:
      </para>

<screen><computeroutput>ELF 32-bit LSB shared object, Intel 80386, version 1 (SYSV), dynamically linked</computeroutput></screen>

      <para>
        Merk <computeroutput>32-bit LSB shared object</computeroutput> delen.
        Et 64-bits bibliotek ville vises som <computeroutput>64-bit LSB shared
        object</computeroutput>.
      </para>
    </note>

  </sect2>

</sect1>
