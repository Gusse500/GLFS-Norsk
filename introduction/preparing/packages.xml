<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="files" xreflabel="Filnedlastinger">
  <?dbhtml filename="files.html"?>

  <title>Filnedlastinger</title>

  <para>På hver side for de fleste pakkene finnes det nedlastingslenker for 
  viktige filer som trengs for installasjonsprosessen. Vanligvis er dette tar 
  filer med kildekode og oppdateringer for å løse diverse problemer. Hvis du 
  følger GLFS rett etter LFS eller MLFS, har du sannsynligvis ikke verktøyene 
  som er nødvendige på LFS målet for å laste ned noen av de nødvendige filene, 
  eller en nettverkstilkobling. Hvis du har det, kan GLFS behandles som BLFS 
  ved at du kan laste ned filer etter hvert, men det kan være ganske upraktisk.</para>

  <para>Uansett kan det være lurt å laste ned alle filene samtidig.</para>

  <para>For det første er det opp til deg hvilken mappe du velger å laste ned 
  til. Men du bør gjøre det i et miljø som har både <xref linkend="wget"/> og
  en nettverkstilkobling. Hvis du følger GLFS i en chroot, kan du laste ned 
  filene i <filename class="directory">$LFS/sources</filename>
  utenfor chrooten, og følg deretter GLFS i chrooten. Hvis du følger GLFS i en 
  TTY, kan du laste ned filene på en annen distribusjon, legge dem på en 
  lagringsenhet og koble lagringsenheten til LFS målet slik at filene er 
  tilgjengelige. Du kan også gjøre en kombinasjon av alt det ovennevnte eller 
  noe annet. Alt dette er hvis du ikke har <xref linkend="wget"/> og
  en nettverkstilkobling på LFS målet. Hvis du har det, kan du følge 
  massenedlastingsprosessen normalt hvor du vil.</para>

  <para>Last ned Wget fillisten, og last deretter ned alle filene som er 
  oppført i den:</para>

<screen><userinput>wget &dl-root;/wget-list &amp;&amp;
wget -N --input-file=wget-list --continue</userinput></screen>

  <note><para>Hvis du har rendert GLFS selv, kan det være lurt å bruke versjonen 
  av <filename>wget-list</filename> som fulgte med renderingen. I så fall, 
  kopier <ulink url="../download/wget-list"/> til mappen du vil laste ned filene 
  til. <literal>file://</literal> eller noe annet kan være prefikset i lenken. 
  Sørg for å slette prefikset først for å sikre at filen blir kopiert. Lenken 
  vil peke til en online kilde hvis du ikke gjenga boken. Lenken bør være unik 
  for gjengivelsen din.</para></note>

  <para>For å bekrefte at alle filer er lastet ned, kan du kjøre 
  følgende:</para>

<screen><userinput>for file in $(grep -v '^#' wget-list); do
  if [ ! -f $(basename $file) ]; then
    echo "$(basename $file) NOT found"
  fi
done</userinput></screen>

  <para>Hvis alle filene ble lastet ned, skal det ikke være noe utdata fra 
  kommandoene ovenfor.</para>

  <important><para>
    Filene for &nvidia; og &cuda; lastes ikke ned fra prosessen ovenfor siden de 
    er store filer og ville vært omtrent 9 GB i seg selv. Imidlertid, filer for 
    pakker som <xref
    linkend="nvidia-vaapi-driver"/> er lastet ned. Du kan laste ned filene nå på 
    forhånd. For å gjøre det, les først <xref
    linkend="nvidia-rev"/>, bestem deg for en større NVIDIA revisjon, og last 
    deretter ned filene på hver side i den revisjonsdelen.
  </para><para>
    Filer for en pakkebunt som <xref linkend="vulkan-sdk"/> og <xref
    linkend="gstreamer-suite"/> vil heller ikke bli lastet ned av instruksjonene 
    ovenfor. De blir i stedet lastet ned når du kommer til disse pakkene 
    buntene, der verktøyene for å laste dem ned skal bli tilgjengelige.
  </para></important>

</sect1>
