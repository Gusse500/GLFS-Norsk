<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY certhost              "https://hg-edge.mozilla.org/">
  <!ENTITY certpath              "/lib/ckfw/builtins/certdata.txt">
  <!ENTITY make-ca-download      "https://github.com/lfs-book/make-ca/archive/v&make-ca-version;/make-ca-&make-ca-version;.tar.gz">
]>

<sect1 id="make-ca" xreflabel="make-ca-&make-ca-version;">
  <?dbhtml filename="make-ca.html"?>

  <title>make-ca-&make-ca-version;</title>
  <indexterm zone="make-ca">
    <primary sortas="a-make-ca">make-ca</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til make-ca</title>

    <para>
      Public Key Infrastructure (PKI) er en metode for å validere autentisiteten 
      til en ellers ukjent enhet på tvers av upålitelige nettverk. PKI fungerer 
      ved å etablere en tillitskjede, i stedet for å stole eksplisitt på hver 
      enkelt vert eller enhet. For at et sertifikat presentert av en ekstern 
      enhet skal være klarert, må sertifikatet presentere en komplett kjede av 
      sertifikater som kan valideres ved hjelp av rotsertifikatet til en 
      sertifiseringsinstans (CA) som er klarert av den lokale maskinen.
    </para>

    <para>
      Å etablere tillit med en CA innebærer å validere ting som firmaadresse, 
      eierskap, kontaktinformasjon osv., og å sikre at CA-en har fulgt beste 
      praksis, som å gjennomgå periodiske sikkerhetsrevisjoner av uavhengige 
      etterforskere og opprettholde en alltid tilgjengelig liste over 
      tilbakekalte sertifikater. Dette er langt utenfor omfanget av BLFS (som 
      det er for de fleste Linux distribusjoner). Sertifikatlageret som er gitt 
      her er hentet fra Mozilla Foundation, som har etablert svært strenge 
      inkluderingsregler beskrevet <ulink
      url="https://www.mozilla.org/en-US/about/governance/policies/security-group/certs/policy/">her</ulink>.
    </para>   

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting (HTTP): <ulink url="&make-ca-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <note>
      <para>
        Denne pakken sender et CA sertifikat for å validere identiteten til
        <ulink url='&certhost;'/>.  Hvis tillitskjeden til dette nettstedet har 
        blitt endret etter utgivelsen av make-ca-&make-ca-version;, kan det 
        hende at det ikke lykkes å få revisjonen av <filename>certdata.txt</filename>
        fra serveren. Bruk en oppdatert make-ca utgivelse på
        <ulink url='https://github.com/lfs-book/make-ca/releases'>the
        release page</ulink> hvis dette problemet oppstår.
      </para>
    </note>

    <bridgehead renderas="sect3">make-ca Avhengigheter</bridgehead>

    <bridgehead renderas="sect4">Påkrevd</bridgehead>
    <para role="required">
      <xref linkend="p11-kit"/>, <xref linkend="libtasn1"/>,
      og <xref role="runtime" linkend="nss"/>
    </para>
    <!-- /usr/bin/trust is needed to extract the certs to /etc/ssl/certs -->

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av make-ca og Generering av CA-sertifikat lager</title>

    <para>
      make-ca skriptet vil laste ned og behandle 
      sertifikatene som er inkludert i <filename>certdata.txt</filename> filen
      til bruk som tillitsankere for <xref linkend="p11-kit"/> tillitsmodulen.
      I tillegg vil den generere systemsertifikatlagre som brukes av BLFS 
      applikasjoner (hvis de anbefalte og valgfrie applikasjonene finnes på 
      systemet). Eventuelle lokale sertifikater lagret i
      <filename>/etc/ssl/local</filename> vil bli importert til både 
      tillitsankrene og de genererte sertifikatlagrene (og overstyre Mozillas 
      tillit). I tillegg vil eventuelle endrede tillitsverdier bli kopiert fra 
      tillitsankrene til <filename>/etc/ssl/local</filename> før noen 
      oppdateringer, som bevarer tilpassede tillitsverdier som avviker fra 
      Mozilla når du bruker <command>trust</command> verktøyet fra
      p11-kit for å operere på tillitslagret.
    </para>

    <para>
      Før du installerer pakken, bør du legge til en ekstra instruksjon til 
      make-ca skript som lager en 
      kompatibilitetssymbolsk lenke som brukes av Debian Linux pakker og er 
      påkrevd av <xref linkend="steam"/> ettersom hovedutviklingen skjer på 
      Debian Linux. Legg til instruksjonen i skriptet nå:
    </para>

<screen><userinput>cat &gt;&gt; make-ca &lt;&lt; "EOF"
<literal>ln -svf /etc/pki/tls/certs/ca-bundle.crt /etc/ssl/certs/ca-certificates.crt
</literal>EOF</userinput></screen>

    <para>
      Nå for å installere make-ca skriptet, må det 
      installeres på riktig sted som &root; bruker:
    </para>

<screen role="root"><userinput>make install &amp;&amp;
install -vdm755 /etc/ssl/local</userinput></screen>

   <note>
     <para>
       Teknisk sett er denne pakken allerede installert på dette tidspunktet. 
       Men de fleste pakkene som lister make-ca som 
       en avhengighet krever faktisk at systemsertifikatlageret er satt opp av 
       denne pakken, i stedet for <command>make-ca</command>
       programmet i seg selv. Så instruksjonene for å bruke
       <command>make-ca</command> for å sette opp systemsertifikatets lagre er 
       inkludert i denne delen.
     </para>
   </note>

   <para>
     Som <systemitem class="username">root</systemitem> bruker,
     last ned sertifikatkilden og klargjør for 
     systembruk med følgende kommando:
   </para>

    <note>
      <para>
        Hvis du kjører skriptet en gang til med samme versjon av
        <filename>certdata.txt</filename>, for eksempel å oppdatere lagret 
        når make-ca oppgraderes, eller å legge til 
        flere butikker etter hvert som nødvendig programvare installeres, 
        erstatt <parameter>-g</parameter> bryteren med
        <parameter>-r</parameter> bryteren i kommandolinjen. Hvis du pakker, 
        kjør <command>make-ca --help</command> for å se alle tilgjengelige 
        kommandolinjealternativer.
      </para>
    </note>

<screen role="root"><userinput>/usr/sbin/make-ca -g</userinput></screen>

  </sect2>

  <sect2 role="configuration" id="make-ca-config">
    <title>Konfigurere make-ca</title>

    <para>
      For de fleste brukere er ingen ytterligere konfigurasjon nødvendig, men 
      standard <filename>certdata.txt</filename> filen levert av make-ca er 
      hentet fra mozilla-release grenen og er modifisert for å gi en Mercurial 
      revisjon. Dette vil være den riktige versjonen for de fleste systemer. Det 
      finnes flere andre varianter av filen tilgjengelig for bruk som kan være 
      foretrukket av en eller annen grunn, inkludert filene som følger med 
      Mozilla produkter i denne boken. RedHat og OpenSUSE bruker for eksempel 
      versjonen som er inkludert i <xref linkend="nss"/>. Ytterligere oppstrøms 
      nedlastinger er tilgjengelige på lenkene som er inkludert i
      <filename>/etc/make-ca/make-ca.conf.dist</filename>. Bare kopier 
      filen til
      <filename>/etc/make-ca.conf</filename> og rediger etter behov.
    </para>

    <indexterm zone="make-ca make-ca-config">
      <primary sortas="e-etc-make-ca-conf">/etc/make-ca.conf</primary>
    </indexterm>

    <bridgehead renderas="sect3">Om tillitsargumenter</bridgehead>

    <para>
      Det finnes tre typer tillit som er anerkjent av
      make-ca skriptet, SSL/TLS, S/Mime, og 
      codeSigning. For OpenSSL, disse er
      <parameter>serverAuth</parameter>,
      <parameter>emailProtection</parameter>, og
      <parameter>codeSigning</parameter> henholdsvis. Hvis ett av de tre 
      tillitsargumentene utelates, er sertifikatet verken klarert eller avvist 
      for den rollen. Klienter som bruker
      OpenSSL eller NSS
      som støter på dette sertifikatet, vil gi en advarsel. 
      Klienter som bruker
      GnuTLS uten
      p11-kit støtten er ikke klar over pålitelige 
      sertifikater. For å inkludere denne CA-en i
      <filename>ca-bundle.crt</filename>,
      <filename>email-ca-bundle.crt</filename>, eller
      <filename>objsign-ca-bundle.crt</filename> filene
      (GnuTLS legacy pakker), må den ha 
      hensiktsmessige tillitsargumenter.
    </para>

    <bridgehead renderas="sect3">Legge til flere CA sertifikater</bridgehead>

    <para>
      <filename class="directory">/etc/ssl/local</filename> mappen er 
      tilgjengelig for å legge til flere CA sertifikater i systemets 
      tillitslager. Denne mappen brukes også til å lagre sertifikater som ble 
      lagt til eller endret i systemets tillitslager av <xref linkend="p11-kit"/> 
      slik at tillitsverdier opprettholdes på tvers av oppgraderinger. Filer i 
      denne mappen må være i OpenSSL klarert 
      sertifikatformat. Sertifikater importert ved hjelp av <command>trust</command> 
      verktøyet fra <xref linkend="p11-kit"/> vil bruke x509 Extended Key Usage 
      verdiene til å tilordne standard tillitsverdier for systemankrene.
    </para>

    <para>Hvis du trenger å overstyre tillitsverdier, eller på annen måte 
      trenger å opprette en OpenSSL klarert sertifikat 
      manuelt fra en vanlig PEM kodet fil, må du legge til tillitsargumenter til
      <command>openssl</command> kommandoen, og opprett et nytt sertifikat. For 
      eksempel, ved å bruke <ulink url="http://www.cacert.org/">CAcert</ulink>
      roots, hvis du vil stole på begge for alle tre rollene, vil følgende 
      kommandoer opprette passende OpenSSL klarerte sertifikater (kjør som
      <systemitem class="username">root</systemitem> bruker etter at <!-- xref
      linkend="wget"/ --> er installert):
    </para>

<screen role="nodump"><userinput>wget http://www.cacert.org/certs/root.crt &amp;&amp;
wget http://www.cacert.org/certs/class3.crt &amp;&amp;
openssl x509 -in root.crt -text -fingerprint -setalias "CAcert Class 1 root" \
        -addtrust serverAuth -addtrust emailProtection -addtrust codeSigning \
        > /etc/ssl/local/CAcert_Class_1_root.pem &amp;&amp;
openssl x509 -in class3.crt -text -fingerprint -setalias "CAcert Class 3 root" \
        -addtrust serverAuth -addtrust emailProtection -addtrust codeSigning \
        > /etc/ssl/local/CAcert_Class_3_root.pem &amp;&amp;

/usr/sbin/make-ca -r</userinput></screen>

    <bridgehead renderas="sect3">Overstyre Mozilla Tillit</bridgehead>

    <para>
      Av og til kan det være tilfeller der du ikke er enig i Mozillas 
      inkludering av en bestemt sertifiseringsinstans. Hvis du vil overstyre 
      standardtilliten til en bestemt CA, kan du ganske enkelt opprette en kopi 
      av det eksisterende sertifikatet i <filename
      class="directory">/etc/ssl/local</filename> med forskjellige 
      tillitsargumenter. Hvis du for eksempel ikke vil stole på 
      "Makebelieve_CA_Root" filen, kjør følgende kommandoer:
    </para>

<screen role="nodump"><userinput>openssl x509 -in /etc/ssl/certs/Makebelieve_CA_Root.pem \
             -text \
             -fingerprint \
             -setalias "Disabled Makebelieve CA Root" \
             -addreject serverAuth \
             -addreject emailProtection \
             -addreject codeSigning \
             > /etc/ssl/local/Disabled_Makebelieve_CA_Root.pem &amp;&amp;

/usr/sbin/make-ca -r</userinput></screen>

  </sect2>

  <sect2 role="configuration" id="make-ca-python">
    <title>Bruke make-ca med Python3</title>

    <para>
      Når Python3 ble installert i LFS/MLFS, den 
      inkluderte pip3 modulen med 
      leverandørsertifikater fra Certifi modulen. Det 
      var nødvendig, det betyr at når <command>pip3</command> brukes kan det 
      referere til disse sertifikatene, hovedsakelig når man oppretter et 
      virtuelt miljø eller når man installerer en modul med alle wheel 
      avhengigheter på én gang.
    </para>

    <para>
      Det er generelt ansett at systemadministratoren bør ha ansvaret for 
      hvilke sertifikater som er tilgjengelige. Nå som <xref
      linkend="make-ca"/> og <xref linkend="p11-kit"/> har blitt installert og
      make-ca har blitt konfigurert, er det mulig å 
      gjøre sånn at <command>pip3</command> bruker systemsertifikatene.
    </para>

    <para>
      De leverandørsertifikatene som er installert i LFS er et øyeblikksbilde 
      fra da den innhentede versjonen når Certifi ble 
      opprettet. Hvis du regelmessig oppdaterer systemsertifikatene, vil 
      leverandørversjonen bli utdatert.
    </para>

    <para>
      For å bruke systemsertifikatene i Python3, bør 
      du sette <envar>_PIP_STANDALONE_CERT</envar> å peke på dem, f.eks. for 
      bash skallet:
    </para>

<screen><userinput>export _PIP_STANDALONE_CERT=/etc/pki/tls/certs/ca-bundle.crt</userinput></screen>

    <warning>
      <para>
        Hvis du har laget virtuelle miljøer, for eksempel når du testet moduler, 
        og disse inkluderer Requests og
        Certifi moduler i <filename
        class="directory">~/.local/lib/python&python3-majorver;/</filename>
        da vil disse lokale modulene bli brukt i stedet for systemsertifikatene 
        med mindre du fjerner de lokale modulene.
      </para>
    </warning>

    <note>
      <para>
        Instruksjonene nedenfor avhenger av filene som er opprettet i
        <xref linkend="bash-profile"/>.
      </para>
    </note>

    <para>
      Sørg nå for at variabelen blir satt ved oppstart ved å opprette følgende 
      Bash oppstartsfil som &root; bruker:
    </para>

<screen role="root"><userinput>cat &gt; /etc/profile.d/pythoncerts.sh &lt;&lt; "EOF"
<literal># Begin /etc/profile.d/pythoncerts.sh

export _PIP_STANDALONE_CERT=/etc/pki/tls/certs/ca-bundle.crt

# End /etc/profile.d/pythoncerts.sh</literal>
EOF</userinput></screen>

    <note><para>
      Hvis du kopierer profilskriptene til et nytt system, må du installere 
      denne pakken før du installerer Python moduler fra noen av LFS bøkene, 
      inkludert denne, slik at sertifikatene
      <command>pip3</command> stoler på kan finnes.
    </para></note>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installert Program</segtitle>
      <segtitle>Installerte Mapper</segtitle>

      <seglistitem>
        <seg>make-ca</seg>
        <seg>/etc/ssl/{certs,local} og
        /etc/pki/{nssdb,anchors,tls/{certs,java}}</seg>
      </seglistitem>
    </segmentedlist>

   <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="make-ca-bin">
        <term><command>make-ca</command></term>
        <listitem>
          <para>
            er et skallskript som tilpasser en gjeldende versjon av
            <filename>certdata.txt</filename>, og forbereder den til bruk 
            som systemets tillitslager
          </para>
          <indexterm zone="make-ca make-ca">
            <primary sortas="b-make-ca">make-ca</primary>
          </indexterm>
        </listitem>
      </varlistentry>
   </variablelist>

  </sect2>

</sect1>
