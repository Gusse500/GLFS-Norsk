<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY curl-download "https://curl.se/download/curl-&curl-version;.tar.xz">
]>

<sect1 id="curl" xreflabel="cURL-&curl-version;">
  <?dbhtml filename="curl.html"?>

  <title>cURL-&curl-version;</title>

  <indexterm zone="curl">
    <primary sortas="a-cURL">cURL</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til cURL</title>

    <para>
      cURL pakken inneholder et verktøy
      og et bibliotek som brukes til å overføre filer med URL-syntaks til hvilken som helst av
      følgende protokoller: DICT, FILE, FTP, FTPS, GOPHER, GOPHERS, HTTP,
      HTTPS, IMAP, IMAPS, LDAP, LDAPS, MQTT, POP3, POP3S, RTSP, SMB, SMBS,
      SMTP, SMPTS, TELNET, and TFTP. Evnen til å både laste ned
      og laste opp filer kan integreres i andre programmer for å støtte
      funksjoner som strømming av media.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&curl-download;"/>
        </para>
      </listitem>
    </itemizedlist>

<!--
    <bridgehead renderas="sect3">Ytterligere nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nødvendig oppdatering:
          <ulink url="&dl-root;/curl/curl-&curl-version;-upstream_fix-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->

    <bridgehead renderas="sect3">cURL Avhengigheter</bridgehead>

    <para role="recommended">
      <xref linkend="libpsl"/> og
      <xref linkend="nghttp2"/> (påkrevd for <xref linkend="rustc"/>)
    </para>

    <note>
      <para>
        De anbefalte avhengighetene, selv om de ikke er strengt nødvendige, 
        anbefales på det sterkeste for å beskytte mot superinformasjonskapsler 
        som medfører sikkerhetsproblemer samt for å få HTTP/2 støtte som noen 
        pakker og nettsteder krever.
      </para>
    </note>

    <para role="optional">
      <xref linkend="libidn2"/>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av cURL</title>

<!--
    <para>
      Først, fiks et problem med en sikkerhetsrettelse:
    </para>

<screen><userinput>sed -i 's/2F 5C/%2F %5C/' scripts/wcurl</userinput></screen>
-->

    <para>
      Installer cURL ved å kjøre følgende kommandoer:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

../configure --prefix=/usr                 \
             --disable-static              \
             --with-openssl                \
             --with-ca-path=/etc/ssl/certs &amp;&amp;

make</userinput></screen>

    <para>
      Nå, som <systemitem class="username">root</systemitem>
      bruker:
    </para>

<screen role="root"><userinput>make install &amp;&amp;

rm -rf docs/examples/.deps &amp;&amp;

find docs \( -name Makefile\* -o  \
             -name \*.1       -o  \
             -name \*.3       -o  \
             -name CMakeLists.txt \) -delete &amp;&amp;

cp -v -R docs -T /usr/share/doc/curl-&curl-version;</userinput></screen>

    <para>
      For å kjøre noen enkle verifiseringstester på den nylig installerte
      <command>curl</command>, utsted følgende kommandoer:
      <command>curl --trace-ascii debugdump.txt https://www.example.com/</command>
      og
      <command>curl --trace-ascii d.txt --trace-time https://example.com/</command>.
      Inspiser de lokalt opprettede sporingsfilene <filename>debugdump.txt</filename>
      og <filename>d.txt</filename>, som inneholder versjonsinformasjon,
      informasjon om nedlastede filer osv. Én fil har logget tiden for hver handling.
    </para>
  </sect2>

  <sect2>
    <title>lib32 Installasjon av cURL</title>
    
    <para>
      Installer lib32-cURL
      ved å kjøre følgende kommandoer:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
CC="gcc -m32" CXX="g++ -m32"           \
PKGCONFIG=i686-pc-linux-gnu-pkg-config \
../configure --prefix=/usr             \
             --libdir=/usr/lib32       \
             --host=i686-pc-linux-gnu  \
             --disable-static          \
             --with-openssl            \
             --without-brotli          \
             --with-ca-path=/etc/ssl/certs &amp;&amp;

make</userinput></screen>

    <para>
      Nå, som <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>make DESTDIR=$PWD/DESTDIR install     &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-subconfigure;

    <para>
      <command>mkdir build &amp;&amp; cd build</command>: Dette er ikke nødvendig
      bare for den vanlige installasjonen, men rengjøring av mappen fører til at
      dokumentasjonen i kildekoden slettes, og denne pakken prøver å
      gjenoppbygge den for lib32 installasjonen, så bygging i en byggemappe tillater
      sletting av bare byggeartefaktene.
    </para>

    &static-libraries;

    <para>
      <parameter>--with-ca-path=/etc/ssl/certs</parameter>: Denne
      bryteren angir plasseringen til BLFS sertifiseringsinstansens lager.
    </para>

    <para>
      <parameter>--with-openssl</parameter>: Denne parameteren velger
      OpenSSL som SSL/TLS implementering.
      Dette alternativet er ikke nødvendig hvis <option>--with-gnutls</option> er
      valgt i stedet.
    </para>

    <para>
      <option>--with-gssapi</option>: Denne parameteren legger til
      Kerberos 5 støtte for
      <filename class="libraryfile">libcurl</filename>.
    </para>

    <para>
      <option>--with-gnutls</option>: Bruk denne bryteren til å
      bygge med GnuTLS støtte
      istedenfor OpenSSL for SSL/TLS. Dette krever at
      <xref linkend="gnutls"/> er installert allerede.
    </para>

    <para>
      <option>--with-ca-bundle=/etc/pki/tls/certs/ca-bundle.crt</option>: Bruk
      denne bryteren i stedet for <parameter>--with-ca-path</parameter> hvis det
      bygges med GnuTLS støtte
      istedenfor OpenSSL for SSL/TLS.
    </para>

    <para>
      <option>--with-libssh2</option>: Denne parameteren legger til
      SSH støtte for cURL. Dette er deaktivert
      som standard.
    </para>

    <para>
      <option>--enable-ares</option>: Denne parameteren legger til støtte for DNS
      løsning gjennom c-ares biblioteket.
    </para>

    <para>
      <command>find docs ... -exec rm {} \;</command>: Denne kommandoen fjerner
      <filename>Makefiles</filename> og manualfiler fra dokumentasjonsmappen
      som ellers ville blitt installert av kommandoene som følger.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installerte programmer</segtitle>
      <segtitle>Installert bibliotek</segtitle>
      <segtitle>Installerte mapper</segtitle>

      <seglistitem>
        <seg>
           curl, curl-config, og wcurl
        </seg>
        <seg>
           libcurl
        </seg>
        <seg>
          /usr/include/curl og
          /usr/share/doc/curl-&curl-version;
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="curl-prog">
        <term><command>curl</command></term>
        <listitem>
         <para>
           er et kommandolinjeverktøy for overføring av filer med URL syntaks
         </para>
          <indexterm zone="curl curl-prog">
            <primary sortas="b-curl">curl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="curl-config">
        <term><command>curl-config</command></term>
        <listitem>
          <para>
            skriver ut informasjon om siste kompilering, som biblioteker
            lenket til og prefiksinnstilling
          </para>
          <indexterm zone="curl curl-config">
            <primary sortas="b-curl-config">curl-config</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="wcurl">
        <term><command>wcurl</command></term>
        <listitem>
         <para>
           er en innpakning rundt cURL som emulerer wget oppførsel
         </para>
          <indexterm zone="curl wcurl">
            <primary sortas="b-wcurl">wcurl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libcurl">
        <term><filename class="libraryfile">libcurl</filename></term>
        <listitem>
          <para>
            tilbyr API funksjonene som kreves av
            <command>curl</command> og andre programmer
          </para>
          <indexterm zone="curl libcurl">
            <primary sortas="c-libcurl">libcurl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
