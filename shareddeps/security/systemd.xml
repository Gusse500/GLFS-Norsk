<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

<!--
  For whenever we move to a stable snapshot for backports
  <!ENTITY systemd-download-http
    "https://anduin.linuxfromscratch.org/LFS/systemd-&systemd-version;-&systemd-stable;.tar.xz">
-->

  <!ENTITY systemd-dl-root "https://github.com/systemd/systemd/archive">
  <!ENTITY systemd-download "&systemd-dl-root;/v&systemd-version;/systemd-&systemd-version;.tar.gz">
]>

<sect1 id="systemd" xreflabel="Systemd-&systemd-version;" revision="systemd">
  <?dbhtml filename="systemd.html"?>


  <title>Systemd-&systemd-version;</title>
  <!-- Whenever we switch back to stable backports, make sure to add the systemd-stable reference back. -->

  <indexterm zone="systemd">
    <primary sortas="a-systemd">systemd</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til Systemd</title>

    <para>
      Selv om Systemd ble installert da LFS ble bygget, er det mange funksjoner 
      som tilbys av pakken som ikke var inkludert i den første installasjonen 
      fordi Linux-PAM ikke var installert ennå. Systemd pakken må bygges opp 
      igjen for å gi en fungerende
      <command>systemd-logind</command> tjeneste, som tilbyr mange 
      tilleggsfunksjoner for avhengige pakker.
    </para>

    &meson-cross;

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&systemd-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Ytterligere nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Påkrevd oppdatering:
          <ulink url="&dl-root;/systemd/systemd-&systemd-version;-buildfix-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>

<!--  Comment out (instead of remove) in case a patch will be needed.
    <bridgehead renderas="sect3">Ytterligere Nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
         Påkrevd oppdatering:
         <ulink url="&patch-root;/systemd-&systemd-version;-upstream_fixes-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->
    <bridgehead renderas="sect3">Systemd Avhengigheter</bridgehead>

    <para role="recommended">
      <xref linkend="linux-pam"/> og
      <xref role="runtime" linkend="polkit"/> (kjøretid)
    </para>

    <note>
      <para>
        <xref linkend='linux-pam'/> er ikke strengt tatt nødvendig for å bygge 
        Systemd, men hovedgrunnen til å gjenoppbygge Systemd i GLFS (det er 
        allerede innebygd i LFS uansett) er for 
        <command>systemd-logind</command> daemon 
        og 
        <filename class='libraryfile'>pam_systemd.so</filename> PAM modulen.
        <xref linkend='linux-pam'/> er nødvendig for dem. Alle pakker i 
        GLFS boken med en avhengighet av Systemd forventer at den er 
        gjenoppbygd med <xref linkend='linux-pam'/>.
      </para>
    </note>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av Systemd</title>

    <para>
      Installer en oppdatering for å gjøre pakken kompatibel med OpenSSL-4:
    </para>

<screen><userinput remap="pre">patch -Np1 -i ../systemd-&systemd-version;-buildfix-1.patch</userinput></screen>

    <para>
      Fjern to unødvendige grupper,
      <systemitem class="groupname">render</systemitem> og
      <systemitem class="groupname">sgx</systemitem>, fra standard udev 
      regler:
    </para>

<screen><userinput remap="pre">sed -i -e 's/GROUP="render"/GROUP="video"/' \
       -e 's/GROUP="sgx", //' rules.d/50-udev-default.rules.in</userinput></screen>

    <warning>
      <para>
        Hvis en tidligere versjon av systemd er installert, fjern en tjeneste 
        som &root; bruker som vil generere feil ved følgende oppstarter:
      </para>
<screen role="root"><userinput remap="pre">rm -v /usr/lib/systemd/system/systemd-update-utmp-runlevel.service</userinput></screen>
    </warning>

    <para>
      Gjenoppbygg Systemd ved å kjøre 
      følgende kommandoer:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr            \
            --buildtype=release      \
            -D default-dnssec=no     \
            -D firstboot=false       \
            -D install-tests=false   \
            -D ldconfig=false        \
            -D man=auto              \
            -D sysusers=false        \
            -D rpmmacrosdir=no       \
            -D homed=disabled        \
            -D mode=release          \
            -D pam=enabled           \
            -D pamconfdir=/etc/pam.d \
            -D dev-kvm-mode=0660     \
            -D nobody-group=nogroup  \
            -D sysupdate=disabled    \
            -D ukify=disabled        \
            -D docdir=/usr/share/doc/systemd-&systemd-version; \
            .. &amp;&amp;

ninja</userinput></screen>

   <para>
     Nå, som &root; bruker:
   </para>

<screen role="root"><userinput>ninja install</userinput></screen>

  </sect2>

  <sect2 role="installation">
    <title>lib32 Installasjon av Systemd</title>

    <para>
      Gjenoppbygg lib32-Systemd ved å kjøre 
      følgende kommandoer:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
LANG=en_US.UTF-8                     \
meson setup --prefix=/usr            \
            --buildtype=release      \
            --cross-file=lib32       \
            -D default-dnssec=no     \
            -D firstboot=false       \
            -D install-tests=false   \
            -D ldconfig=false        \
            -D man=disabled          \
            -D sysusers=false        \
            -D rpmmacrosdir=no       \
            -D homed=disabled        \
            -D userdb=false          \
            -D mode=release          \
            -D pam=enabled           \
            -D pamconfdir=/etc/pam.d \
            .. &amp;&amp;

LANG=en_US.UTF-8 ninja</userinput></screen>

   <para>
     Nå, som &root; bruker:
   </para>

<screen role="root"><userinput>LANG=en_US.UTF-8 DESTDIR=$PWD/DESTDIR ninja install          &amp;&amp;
cp -vR DESTDIR/usr/lib32/security       /usr/lib32           &amp;&amp;
cp -va DESTDIR/usr/lib32/libsystemd.so* /usr/lib32           &amp;&amp;
cp -va DESTDIR/usr/lib32/libudev.so*    /usr/lib32           &amp;&amp;
cp -v  DESTDIR/usr/lib32/pkgconfig/*    /usr/lib32/pkgconfig &amp;&amp;
rm -rf DESTDIR</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-meson;

    &meson-buildtype-release;

    <para>
      <parameter>-D pamconfdir=/etc/pam.d</parameter>: Tvinger PAM filene til å 
      bli installert i /etc/pam.d i stedet for /usr/lib/pam.d.
    </para>

    <para>
      <parameter>-D homed=disabled</parameter>: Fjerner en daemon som ikke 
      tilbyr noen bruk under en tradisjonell GLFS konfigurasjon, spesielt ved 
      bruk av kontoer opprettet med <command>useradd</command>. Avhengighetene 
      som trengs vil ikke bli installert i denne boken.
    </para>

    <para>
      <parameter>-D ukify=disabled</parameter>: Fjerner et skript for å 
      kombinere en kjerne, en initramfs og en kjernekommandolinje osv. inn i et 
      UEFI program som kan lastes inn av UEFI fastvaren for å starte den 
      innebygde Linuxkjernen. Det er ikke nødvendig hvis du fulgte 
      instruksjonene i<ulink url="&blfs-svn;/postlfs/grub-setup.html">BLFS' 
      Grub UEFI Oppsett</ulink>.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Konfigurere Systemd</title>

    <para>
      <filename>/etc/pam.d/system-session</filename> filen må endres 
      og en ny fil må opprettes for at
      <command>systemd-logind</command> skal fungere riktig. Kjør følgende 
      kommandoer som <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>grep 'pam_systemd' /etc/pam.d/system-session ||
cat &gt;&gt; /etc/pam.d/system-session &lt;&lt; "EOF"
<literal># Begin Systemd addition

session  required    pam_loginuid.so
session  optional    pam_systemd.so

# End Systemd addition</literal>
EOF

cat &gt; /etc/pam.d/systemd-user &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/systemd-user

account  required    pam_access.so
account  include     system-account

session  required    pam_env.so
session  required    pam_limits.so
session  required    pam_loginuid.so
session  optional    pam_keyinit.so force revoke
session  optional    pam_systemd.so

auth     required    pam_deny.so
password required    pam_deny.so

# End /etc/pam.d/systemd-user</literal>
EOF</userinput></screen>

    <!-- For some unknown reason if I don't do this, the per-user systemd
         manager fails to start with "Trying to run as user instance,
         but $XDG_RUNTIME_DIR is not set."  This command is enough to
         fix the issue, and it also seems logical to start using the newly
         rebuilt systemd right away (like "exec bash -&dash;login" in LFS),
         so just add it.  -->
    <para>
      Som &root; bruker, erstatt den kjørende <command>systemd</command>
      behandleren (<command>init</command> prosessen) med
      <command>systemd</command> kjørbar som er nybygd og installert:
    </para>

    <screen role='root'><userinput>systemctl daemon-reexec</userinput></screen>

    <note><para>
      Hvis du er i en chroot, vil kommandoen ovenfor bli ignorert med en melding, men 
      den vil ikke returnere en feil. Hvis den kjøres i en Bash økt som avsluttes ved 
      feil, vil ikke kommandoen ovenfor stoppe den. Det er trygt å kjøre den.
    </para></note>

    <para>
      Et skrivebordsmiljø kjører ofte som en gruppe brukertjenester. Disse 
      tjenestene opprettes av per-bruker instanser, i stedet for 
      innloggingsskallet. For å sikre at disse tjenestene får miljøvariablene 
      satt fra <filename
      class='directory'>/etc/profile.d</filename>, som &root; bruker, installer 
      en miljøgenerator for å dumpe alle relevante eksporterte 
      miljøvariabler til per-bruker instanser når instansen 
      starter å kjøre:
    </para>

<screen role='root'><userinput>install -vDm755 /dev/stdin /etc/systemd/user-environment-generators/50-profile.sh &lt;&lt; "EOF"
<literal>#!/usr/bin/env -S -i /usr/bin/bash
# SPDX-License-Identifier: MIT

. /etc/profile

# Systemd should have already set a better value for them.
unset XDG_RUNTIME_DIR
for i in $(locale); do
  unset ${i%=*}
done

# Some shell magic that we don't want to expose.
unset SHLVL

# Systemd does not want to pass functions to the environment
for i in $(declare -pF | awk '{print $3}'); do
  unset -f $i
done

python3 &lt;&lt; _EOF
import os
for var in os.environ:
  # Simply unsetting them in shell does not work.
  if var in ['LC_CTYPE', '_']:
    continue

  print(var + '=' + os.environ[var])
_EOF</literal>
EOF</userinput></screen>

    <note>&install-stdin;</note>

    <para>
      Les <ulink role='man'
      url='&man;systemd.environment-generator.7'>systemd.environment-generator(7)</ulink>
      for detaljer om miljøgeneratorene. 
    </para>

    <note><para>
      Hvis du har redigert innholdet i <filename
      class='directory'>/etc/profile.d</filename> når et skrivebordsmiljø 
	  kjører, må du synkronisere endringene som vanlig bruker (kommandoene 
	  nedenfor må kjøres én gang for hver bruker unntatt &root; og må kjøres 
	  som den brukeren):</para>
<screen role='nodump'><userinput>systemctl --user unset-environment \
  $(/etc/systemd/user-environment-generators/50-profile.sh | sed 's/=.*//')
systemctl --user daemon-reload</userinput></screen></note>

    <important>
      <para>
        Først forsikre at <xref linkend='shadow'/> allerede er gjenoppbygd med
        <xref linkend='linux-pam'/> støtte, deretter logg ut og logg inn igjen. 
        Dette sikrer at den kjørende innloggingsøkten som er registrert med
        <command>systemd-logind</command> og en Systemd instans per bruker som 
        kjører for hver bruker som eier en påloggingsøkt. Mange GLFS pakker som 
        oppgir Systemd som en avhengighet, trenger
        <command>systemd-logind</command> integrasjon og/eller en kjørende 
        Systemd instans per bruker.
      </para>
    </important>

    <warning>
      <para>
        Hvis du oppgraderer fra en tidligere versjon av Systemd og en initrd 
        brukes til systemoppstart, bør du generere en ny initrd før du starter 
        systemet på nytt.
      </para>
    </warning>

  </sect2>

  <sect2 role="content" revision="systemd">
    <title>Innhold</title>

      <para>
        En liste over de installerte filene, sammen med korte 
        beskrivelser, finner du på
        <ulink url="&mlfs-root;/chapter08/systemd.html#contents-systemd"/>.
      </para>

      <para>
        Nedenfor er det nylig installerte biblioteket 
        listet opp sammen med en kort beskrivelse.
      </para>

    <segmentedlist>
      <segtitle>Installert Bibliotek</segtitle>

      <seglistitem>
        <seg>
          pam_systemd
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="pam_systemd">
        <term><filename class="libraryfile">pam_systemd</filename></term>
        <listitem>
          <para>
            er en PAM modul som brukes til å registrere brukerøkter med 
            systemd påloggingsbehandleren,
            <command>systemd-logind</command>
          </para>
          <indexterm zone="systemd pam_systemd">
            <primary sortas="c-pam_systemd">pam_systemd</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
