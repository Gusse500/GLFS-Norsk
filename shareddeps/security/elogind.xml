<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY elogind-root     "https://github.com/elogind/elogind/archive">
  <!ENTITY elogind-download "&elogind-root;/V&elogind-version;/elogind-&elogind-version;.tar.gz">
]>

<sect1 id="elogind" revision="openrc,sysv" xreflabel="elogind-&elogind-version;">
  <?dbhtml filename="elogind.html"?>


  <title>elogind-&elogind-version;</title>

  <indexterm zone="elogind">
    <primary sortas="a-elogind">elogind</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til elogind</title>

    <para>
      elogind er systemd prosjektets «innlogging», 
      utviklet for å være en frittstående daemon. 
      Den integreres med <xref linkend="linux-pam"/>
      å spore alle brukerne som er logget inn på et system, og om de er logget 
      inn grafisk, på konsollen eller eksternt. Elogin eksponerer denne 
      informasjonen via
      standard org.freedesktop.login1 D-Bus
      grensesnittet, og også gjennom filsystemet ved hjelp av systemds standard
      <filename class="directory">/run/systemd</filename> oppsett.
    </para>

    &meson-cross;

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&elogind-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3" revision="openrc">Ytterligere Nedlastinger</bridgehead>
    <itemizedlist spacing="compact" revision="openrc">
      <listitem>
        <para>
          Påkrevd oppdatering:
          <ulink url="&dl-root;/elogind/elogind-&elogind-version;-openrc-user.patch"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">elogind Avhengigheter</bridgehead>

    <para role="recommended">
      <xref role="runtime" revision="openrc,sysv" linkend="dbus-pass1"/>
      (kjøretid),
      <xref linkend="linux-pam"/><phrase revision="openrc,sysv">,</phrase> og
      <xref role="runtime" linkend="polkit"/> (kjøretid)
    </para>

  </sect2>

  <sect2 role="kernel" id="elogind-kernel">
    <title>Kjernekonfigurasjon</title>

    <para>
      Aktiver følgende alternativer i kjernekonfigurasjonen og kompiler 
      kjernen på nytt om nødvendig:
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="elogind-kernel.xml"/>

    <indexterm zone="elogind elogind-kernel">
      <primary sortas="d-elogind">elogind</primary>
    </indexterm>
  </sect2>

  <sect2 role="installation">
    <title>Installasjon av elogind</title>

    <para revision="openrc">
      Det er spesiell håndtering for <literal>openrc-user</literal>, en hjelper 
      for OpenRC som håndterer brukertjenester, som ødelegger funksjonaliteten 
      og hevder at nødvendige variabler ikke er satt. Rett oppførselen:
    </para>

<screen revision="openrc"><userinput>patch -Np1 -i ../elogind-&elogind-version;-openrc-user.patch</userinput></screen>

    <para>
      Installer elogind ved å kjøre følgende 
      kommandoer:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr                          \
            --buildtype=release                    \
            -D docdir=/usr/share/doc/elogind-&elogind-version; \
            -D cgroup-controller=elogind           \
            -D dev-kvm-mode=0660                   \
            -D dbuspolicydir=/etc/dbus-1/system.d  \
            .. &amp;&amp;
ninja</userinput></screen>

    <para>
      Nå, som <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>ninja install                                           &amp;&amp;
ln -sfv  libelogind.pc /usr/lib/pkgconfig/libsystemd.pc &amp;&amp;
ln -sfvn elogind /usr/include/systemd</userinput></screen>

  </sect2>

  <sect2 role="installation">
    <title>lib32 Installasjon av elogind</title>

    <para>
      Installer lib32-elogind ved å kjøre følgende 
      kommandoer:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
meson setup --prefix=/usr                         \
            --buildtype=release                   \
            --cross-file=lib32                    \
            -D cgroup-controller=elogind          \
            -D dev-kvm-mode=0660                  \
            -D dbuspolicydir=/etc/dbus-1/system.d \
            .. &amp;&amp;
ninja</userinput></screen>

    <para>
      Nå, som <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>DESTDIR=$PWD/DESTDIR ninja install                       &amp;&amp;
cp -vR DESTDIR/usr/lib32/* /usr/lib32                    &amp;&amp;
rm -rf DESTDIR                                           &amp;&amp;
ln -sfv libelogind.pc /usr/lib32/pkgconfig/libsystemd.pc &amp;&amp;
ldconfig</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-meson;

    <!--
    <para>
      <command>sed ... meson.build</command>: Denne endringen tillater at 
      pakken kan bygges uten at polkit er installert (det er fortsatt en 
      kjøretids avhengighet), men at polkit kan brukes etter at pakken er 
      installert.
    </para>

    <para>
      <command>sed ... src/login/logind.c</command>: Denne endringen tillater 
      at elogind daemonen avsluttes når den kobles fra dbus (for eksempel når 
      dbus avsluttes).
    </para>
    -->
    <para>
      <parameter>-D docdir=/usr/share/doc/elogind-&elogind-version;</parameter>:
      Dette er nødvendig for å installere dokumentasjon i en versjonsmappe.
    </para>

    <para>
      <parameter>-D cgroup-controller=elogind</parameter>: Denne bryteren er 
      nødvendig for å bygge denne pakken når kjernen ikke er bygget med
      <option>CONFIG_CGROUPS</option> aktivert. Merk at 
      elogind strengt tatt trenger 
      en kjerne med <option>CONFIG_CGROUPS</option> aktivert under kjøring, 
      men denne bryteren vil tillate bygging av pakken først.
    </para>

    <para>
      <parameter>-D dbuspolicydir=/etc/dbus-1/system.d</parameter>: Denne 
      bryteren angir plasseringen til D-Bus-policy 
      mappen.
    </para>

    <para>
      <parameter>-D dev-kvm-mode=0660</parameter>: LFS udev regelen angir 
      modusen til <filename class='devicefile'>/dev/kvm</filename> til 0660.
      Dette alternativet sikrer at elogind udev reglene er konsistente med 
      LFS konfigurasjonen.
    </para>

    <para>
      <option>-D default-kill-user-processes=false</option>: Bestemmer om 
      prosessene til en bruker skal avsluttes når brukeren logger ut. 
      Standardverdien er <emphasis>true</emphasis>, men dette motvirker den 
      tradisjonelle bruken av <command>screen</command> eller <command>tmux</command>. 
      Dette kan også endres i konfigurasjonsfilen (se nedenfor).
    </para>

    <para>
      <command>ln -s ...</command>: Disse kommandoene installerer symbollenker slik 
      at programvarepakker kan finne det systemd kompatible biblioteket og deklarasjonene.
    </para>

  </sect2>

  <sect2 role="configuration">
  <title>Konfigurere elogind</title>

    <sect3 id="elogind-config">
      <title>Konfigurasjonsfil</title>
      <para>
        <filename>/etc/elogind/logind.conf</filename>
      </para>

      <indexterm zone="elogind elogind-config">
        <primary
        sortas="e-etc-elogind-logind.conf">/etc/elogind/logind.conf</primary>
      </indexterm>
    </sect3>

    <sect3><title>Konfigurasjonsinformasjon</title>

      <para>
        Den installerte filen <filename>/etc/elogind/logind.conf</filename>
        inneholder alle mulige alternativer med standardverdiene, kommentert ut. 
        Du kan ønske å deaktivere automatisk avslutning av brukerprosesser når 
        brukeren logger ut, ved å kjøre, som 
        <systemitem class="username">root</systemitem> bruker:
      </para>

<screen role="root"><userinput>sed -e '/\[Login\]/a KillUserProcesses=no' \
    -i /etc/elogind/logind.conf</userinput></screen>

      <para>
        Hver bruker må registrere en brukerøkt ved hjelp av 
        Linux-PAM ved innlogging.
        <filename>/etc/pam.d/system-session</filename> filen må endres, 
        og en ny fil må opprettes for at
        <command>elogind</command> for at den skal fungere riktig. Kjør følgende 
        kommandoer som <systemitem class="username">root</systemitem> bruker:
      </para>

<screen role="root"><userinput>cat &gt;&gt; /etc/pam.d/system-session &lt;&lt; "EOF" &amp;&amp;
<literal># Begin elogind addition

session  required    pam_loginuid.so
session  optional    pam_elogind.so

# End elogind addition</literal>
EOF
cat &gt; /etc/pam.d/elogind-user &lt;&lt; "EOF"
<literal># Begin /etc/pam.d/elogind-user

account  required    pam_access.so
account  include     system-account

session  required    pam_env.so
session  required    pam_limits.so
session  required    pam_unix.so
session  required    pam_loginuid.so
session  optional    pam_keyinit.so force revoke
session  optional    pam_elogind.so

auth     required    pam_deny.so
password required    pam_deny.so

# End /etc/pam.d/elogind-user</literal>
EOF</userinput></screen>

    <note>
      <para>Fortsett nå til du kommer til <xref linkend="dbus"/>,
      installer den, og følg konfigurasjonsinstruksjonene på den siden for å se 
      om elogind fungerer som den skal.</para>
    </note>

    </sect3>
  </sect2>

  <sect2 role="content" revision="openrc,sysv">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installerte Programmer</segtitle>
      <segtitle>Installert Bibliotek</segtitle>
      <segtitle>Installerte Mapper</segtitle>

      <seglistitem>
        <seg>
          busctl,
          elogind-inhibit, og
          loginctl
        </seg>
        <seg>
          libelogind
        </seg>
        <seg>
          /usr/lib/elogind,
          /etc/elogind,
          /usr/include/elogind, og
          /usr/share/doc/elogind-&elogind-version;
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="busctl">
        <term><command>busctl</command></term>
        <listitem>
          <para>
            brukes til å introspektere og overvåke D-Bus bussen
          </para>
          <indexterm zone="elogind busctl">
            <primary sortas="b-busctl">busctl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="elogind-inhibit">
        <term><command>elogind-inhibit</command></term>
        <listitem>
          <para>
            brukes til å kjøre et program med en avstengnings, hvile eller 
            inaktiv funksjon inhibitorlås aktivert
          </para>
          <indexterm zone="elogind elogind-inhibit">
            <primary sortas="b-elogind-inhibit">elogind-inhibit</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="loginctl">
        <term><command>loginctl</command></term>
        <listitem>
          <para>
            brukes til å introspektere og kontrollere tilstanden til elogind 
            påloggingsadministratoren.
          </para>
          <indexterm zone="elogind loginctl">
            <primary sortas="b-loginctl">loginctl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libelogind">
        <term><filename class="libraryfile">libelogind</filename></term>
        <listitem>
          <para>
            er det viktigste elogind verktøybiblioteket
          </para>
          <indexterm zone="elogind libelogind">
            <primary sortas="c-libelogind">libelogind</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
