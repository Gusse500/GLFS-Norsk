<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY dbus-download "https://dbus.freedesktop.org/releases/dbus/dbus-&dbus-version;.tar.xz">
]>

<sect1 id="dbus-pass1" xreflabel="D-Bus-&dbus-version; - Pass 1" revision="sysv">
  <?dbhtml filename="dbus-pass1.html"?>

  <title>D-Bus-&dbus-version; - Pass 1</title>
  
  <indexterm zone="dbus-pass1">
    <primary sortas="a-dbus-pass1">D-Bus-Pass1</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til D-Bus - Pass 1</title>

    <para>
      D-Bus er et meldingsbussystem, en enkel måte 
      for applikasjoner å kommunisere med hverandre.
      D-Bus leverer både en systemdaemon (for 
      hendelser som <quote>ny maskinvareenhet lagt til</quote> eller 
      <quote>skriverkøen er endret</quote>) og en daemon for per-bruker 
      påloggingsøkt (for generelle IPC behov blant brukerapplikasjoner). 
      Meldingsbussen er også bygget oppå et generelt én-til-én 
      meldingsoverføringsrammeverk, som kan brukes av to applikasjoner til å 
      kommunisere direkte (uten å gå gjennom meldingsbussdaemonen).
    </para>

    <para>
      Dette er første gjennomgang. Et nyttig program., <command>dbus-launch</command>,
      vil bli kompilert senere, men er ikke nødvendig nå. Det avhenger av at
      <xref linkend="xorg7-lib"/> installeres på forhånd.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&dbus-download;"/>
        </para>
      </listitem>
    </itemizedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av D-Bus - Pass 1</title>

    &dbus-m64;

    <para>
      Generer D-Bus UUID for å unngå 
      advarsler når du kompilerer noen pakker med følgende kommando som
      <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>dbus-uuidgen --ensure</userinput></screen>

    <para>
      Hvis du bruker elogind, opprett en symbollenke til
      <filename>/var/lib/dbus/machine-id</filename> filen:
    </para>

<screen role="root"><userinput>ln -sfv /var/lib/dbus/machine-id /etc</userinput></screen>

  </sect2>

  <sect2>
    <title>lib32 Installasjon av D-Bus</title>

    <note>
      <para>
        Programmet <command>dbus-launch</command> trenger ikke å ha en 
        lib32 versjon, så denne lib32-kompilasjonen vil være den første og 
        eneste nødvendige prosessen med mindre du oppgraderer D-Bus.
      </para>
    </note>

    <para>
      Installer lib32-D-Bus ved å kjøre følgende kommandoer:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
CC="gcc -m32" CXX="g++ -m32"           \
PKG_CONFIG_PATH="/usr/lib32/pkgconfig" \
meson setup --prefix=/usr              \
            --libdir=/usr/lib32        \
            --wrap-mode=nofallback     \
            -D systemd=disabled        \
            .. &amp;&amp;

ninja</userinput></screen>

    <para>
      Nå som &root; bruker:
    </para>

<screen role="root"><userinput>DESTDIR=$PWD/DESTDIR ninja install    &amp;&amp;
cp -vR DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-meson;

    &meson-wrap-nofallback;

    <para>
      <parameter>-D systemd=disabled</parameter>: Ikke installer systemd 
      støttefiler selv om <filename class="libraryfile">libsystemd</filename> 
      finnes (dvs. <xref linkend="elogind"/> pakken er installert). 
    </para>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <para>
      Se hele innholdslisten på <xref linkend="dbus"/>.
    </para>

  </sect2>

</sect1>
