<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="gpuconfig" xreflabel="GPU Kjernealternativer og Fastvare">
  <?dbhtml filename="gpuconfig.html"?>


  <title>GPU Kjernealternativer og Fastvare</title>

  <para>
    Linuxkjernen har flere drivere for alle typer maskinvare, inkludert 
    GPU-er, og de fleste drivere med åpen kildekode finnes i kjernen. 
    Dette inkluderer AMD, i915 og Nouveau. Det finnes også andre drivere 
    som kan være nyttige for visse brukere. Fastvare er også nødvendig 
    for mange drivere.
  </para>

  <sect2 role="kernel" id="gpu-kernel" xreflabel='GPU Kernel Configuration'>
    <title>Kjernekonfigurasjon</title>

    <para>
      Kjernealternativene nedenfor aktiverer GPU og virtuelle GPU drivere. 
      Aktiver de du trenger, og kompiler kjernen på nytt om nødvendig.
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude" href="gpuconfig-kernel.xml"/>

    <note>
    <para>
      Det finnes kommentarer til alternativene ovenfor, som samsvarer med 
      Mesas Gallium3D dri drivernavn.
    </para>

    <para>
      For GPU-er som støttes av Iris Gallium3D driveren, med
      <option>CONFIG_DRM_I915</option> aktivert og
      <option>CONFIG_DRM_XE_FORCE_PROBE</option> satt til 
      strengen <literal>*</literal>, i915 driveren vil bli brukt hvis den 
      støtter GPU-en, og Xe driveren vil bli brukt hvis den støtter GPU-en, 
      men i915-driveren ikke gjør det. Hvis du er sikker på at GPU-en din 
      støttes av i915 driveren, kan du deaktivere Xe driveren for å spare 
      byggetid. Hvis GPU-en din støttes av begge driverne og du vil bruke 
      Xe driveren, kan du deaktivere i915 driveren, eller få i915 driveren 
      til å nekte GPU-en din via
      <option>CONFIG_DRM_I915_FORCE_PROBE</option> alternativet, for eksempel å 
      sette den til <literal>!7d55</literal> ville få i915 til å nekte 
      Meteor Lake integrert GPU (hvorav PCI ID er
      <literal>8086:7d55</literal>), som overlater den til Xe driveren.
    </para>

    <para>
      Å velge <option>CONFIG_DRM_RADEON</option>,
      <option>CONFIG_DRM_AMDGPU</option>,
      <option>CONFIG_DRM_NOUVEAU</option>,
      <option>CONFIG_DRM_I915</option>, eller
      <option>CONFIG_DRM_XE</option> til
      <quote><literal>y</literal></quote> anbefales ikke. Hvis det er tilfelle, 
      må all nødvendig fastvare bygges som en del av kjernebildet eller 
      initramfs for at driveren skal fungere riktig.
    </para>

    <para>
      Underpostene under <option>CONFIG_DRM_AMDGPU</option> brukes for å 
      sikre at AMDGPU kjernedriveren støtter alle GPU-er som bruker
      <literal>radeonsi</literal> driveren.  De er ikke nødvendige hvis du 
      ikke trenger <option>CONFIG_DRM_AMDGPU</option>. 
      De kan være unødvendige for enkelte GPU-modeller.
    </para>

    <para>
      For <literal>LLVMPipe</literal> eller <literal>LLVMPipe</literal>,
      <option>CONFIG_DRM_VGEM</option> er nødvendig, ellers vil Xorg klienter 
      sannsynligvis mislykkes med en kryptisk melding <computeroutput>
      Error: couldn't get an RGB, Double-buffered visual</computeroutput>.
      Selv om dette kan kompileres som en modul, er det mer praktisk å 
      bare bygge det inn i kjernen, ellers vil ikke modulen lastes 
      inn automatisk.
    </para>
    </note>

    <indexterm zone="gpuconfig gpu-kernel">
      <primary sortas="d-gpuconfig">gpuconfig</primary>
    </indexterm>

  </sect2>

  <sect2>
    <title>Fastvare</title>

    <sect3>
      <title>ATI Fastvare</title>

      <para>
        ATI Radeon GPU-er krever fastvare for å kunne bruke KMS (kernel 
        modesetting – det foretrukne alternativet) samt for Xorg. 
        Fastvaren er i
        <ulink url="https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git">linux-firmware</ulink>.
      </para>

      <para>
        For å spare plass anbefales det å bruke
        <xref linkend="pciutils"/> og sin pakket binærfil <command>
        lspci</command> for å identifisere hva VGA kontrolleren er. 
        Etter at du har funnet ut det, sjekk RadeonFeature siden på Xorg 
        wikien  
        <ulink url="https://wiki.x.org/wiki/RadeonFeature/#index5h2">
        Decoder ring for engineering vs marketing names</ulink> for å 
        identifisere familien (du må kanskje vite dette for Xorg driveren 
        – Southern Islands og Sea Islands bruker Radeonsi driveren) og den 
        spesifikke modellen.
      </para>

      <para>
        Nå som du vet hvilken kontroller du bruker, se på
        <ulink url="https://wiki.gentoo.org/wiki/Radeon#Firmware">
        Radeon Firmware</ulink> side på Gentoo wikien som har en tabell som 
        viser de nødvendige firmware-blobene for de forskjellige brikkesettene. 
        Merk at Southern Islands og Sea Islands brikkene bruker forskjellig 
        firmware for kjerne 3.17 og senere sammenlignet med tidligere kjerner. 
        Identifiser og last ned de nødvendige blobene og deretter 
        installer dem:
      </para>

<screen role="root"><userinput>mkdir -pv /usr/lib/firmware/radeon &amp;&amp;
cp -v &lt;YOUR_BLOBS&gt; /usr/lib/firmware/radeon</userinput></screen>

    </sect3>

    <sect3>
      <title>RadeonSI Fastvare</title>

      <para>
        AMDGPU enheter krever generelt fastvare for å kunne brukes.
        Installer <xref linkend="pciutils"/> for å sjekke modellnavnet på 
        kortet ditt, og se etter «VGA kompatibel kontroller:». Hvis du har en 
        APU (Accelerated Processing Unit, dvs. CPU og video på samme brikke) 
        vil det sannsynligvis fortelle deg navnet. Hvis du har et separat 
        amdgpu grafikkkort, må du søke for å finne ut hvilket navn det bruker 
        (f.eks. et kort beskrevet som Advanced Micro Devices, Inc. [AMD/ATI] 
        Baffin [Radeon RX 550 640SP / RX 560/560X] trenger Polaris11 fastvare. 
        Det finnes en tabell over «Familie, brikkesettnavn, produktnavn og 
        fastvare» på slutten av kjerneseksjonene i <ulink
        url="https://wiki.gentoo.org/wiki/AMDGPU#Known_firmware_blobs">AMDGPU
        </ulink> siden på Gentoo wikien.
      </para>

      <para>
        Når du har identifisert fastvarenavnet, installerer du alle relevante 
        filer for det. For eksempel har Baffin kortet nevnt ovenfor 21 
        forskjellige polaris11* filer, APU-er som Renoir og Picasso har minst 
        12 filer og kan få flere i fremtidige oppdateringer (f.eks. har Raven 
        APU-en nå en trettende fil, raven_ta.bin).
      </para>

<screen role="root"><userinput>mkdir -pv /usr/lib/firmware/amdgpu &amp;&amp;
cp -v &lt;YOUR_BLOBS&gt; /usr/lib/firmware/amdgpu</userinput></screen>

      <para>
        Hvis diskplass ikke er et problem, kan du installere alle de nåværende 
        amdgpu fastvarefilene og ikke bekymre deg for nøyaktig hvilket 
        brikkesett som er installert.
      </para>

    </sect3>

    <sect3>
      <title>i915 Fastvare</title>

      <para>
        Intels integrerte GPU-er fra Skylake og utover kan bruke fastvare for 
        GuC (grafikkmikrokontrolleren), og også for HuC (HEVC/H265 
        mikrokontroller som avlaster til GPU-en) og DMC (skjermmikrokontroller) 
        for å gi ytterligere lavstrøms tilstander. GuC og HuC har hatt en 
        varierende historikk i kjernen, og oppdatert fastvare kan være 
        deaktivert som standard, avhengig av kjerneversjonen din. 
        Ytterligere detaljer finner du på
        <ulink url="https://01.org/linuxgraphics/downloads/firmware/"/> og
        <ulink url="https://wiki.archlinux.org/index.php/intel_graphics"/>.
      </para>

    </sect3>

    <sect3>
      <title>NVIDIA Fastvare</title>

      <para>
        Noen NVIDIA GPU-er fra Kepler og utover, inkludert RTX og GTX 9xx serien 
        og utover, krever firmware signert av NVIDIA for å fungere. NVIDIA har 
        nå gitt ut firmware opp til Blackwell (50xx) til
        <ulink url="https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git">linux-firmware</ulink>.
        Les
        <ulink url='https://nouveau.freedesktop.org/CodeNames.html'>the
        CodeNames page of the Nouveau wiki</ulink> for å finne ut kodenavnet på 
        NVIDIA GPU-en din (for eksempel kodenavnet til GeForce RTX 5080 er 
        GB203). Mappen for fastvarefilene du trenger heter som et resultat av 
        konverteringen av kodenavnet til små bokstaver
        (<filename class='directory'>gb203</filename> (for eksempelet). Mappen 
        (eller noe av innholdet) kan faktisk være en symbollenke til en annen 
        mappe (eller noen filer i en annen mappe).
      </para>

      <para>
        <filename>WHENCE</filename> filen i <filename class="directory">
        linux-firmware</filename> mappen dokumenterer alle fastvarefilene og 
        potensielle symbolske lenker. Når du har funnet det du trenger, slå det 
        opp i <filename>WHENCE</filename> og lag de nødvendige symbolske 
        lenkene. Hvis du ikke finner GPU-en din oppført i <filename>WHENCE</filename>, 
        betyr det at GPU-en din enten ikke trenger fastvare, eller at fastvaren 
        ikke har blitt utgitt ennå. Hvis du ikke vil bry deg og heller vil 
        installere all fastvaren og symbolske lenker, følg instruksjonene 
        nedenfor for å installere den nyere fastvaren automatisk og riktig:
      </para>

<screen role="root"><userinput>git clone https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git  &amp;&amp;
cd linux-firmware                                                                      &amp;&amp;
mkdir -pv /usr/lib/firmware/nvidia                                                     &amp;&amp;
mkdir -pv DESTDIR/usr/lib/firmware                                                     &amp;&amp;
sed -i 's@^destdir=$@destdir=DESTDIR/usr/lib/firmware@' copy-firmware.sh               &amp;&amp;
sh copy-firmware.sh                                                                    &amp;&amp;
cp -vr DESTDIR/usr/lib/firmware/nvidia/* /usr/lib/firmware/nvidia                      &amp;&amp;
rm -rf DESTDIR</userinput></screen>

      <para>
        For NVIDIA GPU-er fra Tesla til Kepler krever Nouveau driveren noe 
        ekstra fastvare for å støtte akselerasjon av videodekoding. Disse 
        fastvarefilene er ikke utgitt av NVIDIA, men de kan hentes ut fra 
        NVIDIA kjørefilen:
      </para>

<screen role="root"><userinput>wget https://anduin.linuxfromscratch.org/BLFS/nvidia-firmware/extract_firmware.py        &amp;&amp;
wget https://us.download.nvidia.com/XFree86/Linux-x86/340.32/NVIDIA-Linux-x86-340.32.run &amp;&amp;
sh NVIDIA-Linux-x86-340.32.run --extract-only                                            &amp;&amp;
python3 extract_firmware.py                                                              &amp;&amp;
mkdir -p /usr/lib/firmware/nouveau                                                       &amp;&amp;
cp -d nv* vuc-* /usr/lib/firmware/nouveau/</userinput></screen>

    </sect3>

  </sect2>

</sect1>
