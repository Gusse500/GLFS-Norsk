<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY rustc-download "https://static.rust-lang.org/dist/rustc-&rustc-version;-src.tar.xz">
]>

<sect1 id="rustc" xreflabel="rustc-&rustc-version;">
  <?dbhtml filename="rustc.html"?>

  <title>Rustc-&rustc-version;</title>

  <indexterm zone="rustc">
    <primary sortas="a-rustc">Rustc</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til Rustc</title>

    <para>
      Programmeringsspråket Rust er designet for å være et trygt, samtidig og 
      praktisk språk.
    </para>

    <para>
      Som med mange andre programmeringsspråk,
      Rustc (Rust kompilatoren) trenger en binærfil 
      som den kan starte opp fra. Den laster ned en stage0 binærfil ved starten 
      av byggingen, så du kan ikke kompilere den uten en internettforbindelse.
    </para>

    <note>
      <para>
        Selv om GLFS vanligvis installeres i <filename
        class="directory">/usr</filename>, når du senere oppgraderer til en 
        nyere versjon av Rustc, de gamle bibliotekene i 
        <filename class="directory">/usr/lib/rustlib</filename> vil forbli, med 
        forskjellige hasher i navnene sine, men vil ikke være brukbare og vil 
        kaste bort plass. Redaktørene anbefaler å plassere filene i <filename
        class="directory">/opt</filename> mappen.  Spesielt hvis du har grunn 
        til å gjenoppbygge med en modifisert konfigurasjon (f.eks. ved å bruke 
        den medfølgende LLVM-en etter å ha bygget med delt LLVM, kanskje for å 
        kompilere crates for arkitekturer som GLFS LLVM bygget ikke støtter), 
        er det mulig at installasjonen etterlater et ødelagt
        <command>cargo</command> program. I en slik situasjon, fjern enten den 
        eksisterende installasjonen først, eller bruk et annet prefiks, for 
        eksempel /opt/rustc-&rustc-version;-build2.
      </para>

      <para>
        Hvis du foretrekker det, kan du selvfølgelig endre prefikset til 
        <filename class="directory">/usr</filename>.
      </para>
    </note>

    <para>
      Den nåværende rustbuild byggesystemet vil bruke 
      alle prosessorer, selv om det ikke skalerer godt og ofte faller tilbake 
      til å bare bruke én kjerne mens det venter på at et bibliotek skal 
      kompileres. Det kan imidlertid stort sett begrenses til et spesifisert 
      antall prosessorer ved en kombinasjon av å legge til bryteren 
      <literal>--jobs &lt;N&gt;</literal> (f.eks. '--jobs 4' å begrense til 4 
      prosessorer) på hver aktivering av <command>./x.py</command> <emphasis>og
      </emphasis> ved hjelp av en miljøvariabel <envar>CARGO_BUILD_JOBS=&lt;N&gt;</envar>. 
    </para>

    <para>
      Den nåværende versjonen av rusts num_cpus kart gjenkjenner nå at cgroups 
      kan brukes til å begrense hvilke prosessorer den har lov til å bruke. Så 
      hvis din maskin mangler DRAM (vanligvis mindre enn 2 GB DRAM per kjerne), 
      kan det være et alternativ å koble CPU-er fra.
      <!--
      Read <xref linkend='build-in-cgroup'/> for how to use a cgroup.
      -->
    </para>

    <para>
      For øyeblikket gir ikke Rust noen garantier for et stabilt ABI.
    </para>

    <note>
      <para>
        Rustc standardinnstillingen er å bygge for 
        ALLE støttede arkitekturer, ved å bruke en levert kopi av 
        LLVM. I GLFS er byggingen kun for X86 
        arkitekturfamilien. Hvis du har tenkt å utvikle Rust crates, er kanskje 
        ikke denne byggingen god nok for dine formål.
      </para>
      <para>
        Byggetidene for denne versjonen når den gjentas på samme maskin er ofte 
		rimelig konsistente, men som med alle kompileringer som bruker
        <command>rustc</command> det kan være noen veldig langsomme avvikere.
      </para>
    </note>

    &build-use-internet;

    &long-build-time;

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&rustc-download;"/>
        </para>
      </listitem>
    </itemizedlist>

<!--
    <bridgehead renderas="sect3">Ytterligere Nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Påkrevd oppdatering:
          <ulink url="&dl-root;/rustc/rustc-&rustc-version;-upstream_fixes_llvm-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->

    <bridgehead renderas="sect3">Rustc Avhengigheter</bridgehead>

    <para role="required">
      <xref linkend="cmake"/> og
      <!-- Required for downloading stage 0 binaries.
           Otherwise it's recommended (if not installed, a vendored copy
           will be built). -->
      <xref linkend="curl"/>
    </para>

    <para role="recommended">
      <xref linkend="llvm"/>
      (bygget med <parameter>-D LLVM_LINK_LLVM_DYLIB=ON</parameter> sånn at
      Rustc kan lenke til det systeminstallerte
      LLVM i stedet for å bygge den leverte versjonen)
    </para>

    <note>
      <para>
        Hvis en anbefalt avhengighet ikke er installert, vil en levert kopi i
        Rustc kildekode tarballen vil bli bygget og brukt.
      </para>
    </note>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av Rustc</title>

    <para>
      For å installere i
      <filename class="directory">/opt</filename> mappen, fjern eventuelle
      eksisterende <filename>/opt/rustc</filename> symbolkoblinger
      og opprett en ny mappe (dvs. med et annet navn hvis du prøver en 
      modifisert versjon av samme versjon).
      Som <systemitem class="username">root</systemitem>
      bruker:
    </para>

<screen role="root"><userinput>mkdir -pv /opt/rustc-&rustc-version; &amp;&amp;
ln -svfn rustc-&rustc-version; /opt/rustc</userinput></screen>

    <note>
      <para>
        Hvis flere versjoner av Rustc er installert
        i <filename class="directory">/opt</filename>, å bytte til en annen 
        versjon krever bare å endre <filename> /opt/rustc</filename>
        symbolske lenke og deretter kjøre <command>ldconfig</command>.
      </para>
    </note>

    <para>
      Lag en passende <filename>bootstrap.toml</filename> fil som vil 
      konfigurere byggingen.
    </para>

<screen><userinput>cat &gt; bootstrap.toml &lt;&lt; "EOF" &amp;&amp;
<literal># See bootstrap.toml.example for more possible options,
# and see src/bootstrap/defaults/bootstrap.dist.toml for a few options
# automatically set when building from a release tarball.
# We have to override a decent number of them.

# Tell x.py the editors have reviewed the content of this file
# and updated it to follow the major changes of the building system,
# so x.py will not warn us to do such a review.
change-id = &rustc-change-id;

[llvm]
# When using system llvm prefer shared libraries
link-shared = true
</literal>
EOF
if [ ! -f /usr/lib32/libc.so.6 ]; then
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF"
<literal># If building the shipped LLVM source, only enable the x86 target
# instead of all the targets supported by LLVM.
targets = "X86"
</literal>
EOF
fi
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF"
<literal>[build]
description = "for GLFS &version;"</literal>
EOF
if [ -f /usr/lib32/libc.so.6 ]; then
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF"
<literal>target = [
  "x86_64-unknown-linux-gnu",
  "i686-unknown-linux-gnu",
]</literal>
EOF
fi &amp;&amp;
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF" &amp;&amp;
<literal>
# omit docs to save time and space (default is to build them)
docs = false

# Do not query new versions of dependencies online.
locked-deps = true

# Specify which extended tools (those from the default install).
tools = ["cargo", "clippy", "rustdoc", "rustfmt"]

[install]
prefix = "/opt/rustc-&rustc-version;"
docdir = "share/doc/rustc-&rustc-version;"

[rust]
channel = "stable"

# Uncomment if FileCheck has been installed.
#codegen-tests = false

# If you didn't build in NVPTX support, you can uncomment this.
#llvm-bitcode-linker = false

# Enable the same optimizations as the official upstream build.
lto = "thin"
codegen-units = 1
</literal>
EOF
if [ -f /usr/lib32/libc.so.6 ]; then
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF"
<literal>[target.x86_64-unknown-linux-gnu]
cc = "/usr/bin/gcc"
cxx = "/usr/bin/g++"
ar = "/usr/bin/gcc-ar"
ranlib = "/usr/bin/gcc-ranlib"
llvm-config = "/usr/bin/llvm-config"

[target.i686-unknown-linux-gnu]
cc = "/usr/bin/gcc"
cxx = "/usr/bin/g++"
ar = "/usr/bin/gcc-ar"
ranlib = "/usr/bin/gcc-ranlib"</literal>
EOF
else
cat &gt;&gt; bootstrap.toml &lt;&lt; "EOF"
<literal>[target.x86_64-unknown-linux-gnu]
llvm-config = "/usr/bin/llvm-config"

[target.i686-unknown-linux-gnu]
llvm-config = "/usr/bin/llvm-config"</literal>
EOF
fi</userinput></screen>

    <note>
      <para>
        Kommandoene ovenfor vil opprette bootstrap.toml forskjellig avhengig av 
        om noen få kontroller består/feiler. Dette ble gjort for å unngå forvirring.
      </para>
    </note>

<!--
    <para>
      Bruk nå en oppdatering for å fikse bygging med <xref linkend="llvm"/>:
    </para>

<screen><userinput>patch -Np1 -i ../rustc-&rustc-version;-upstream_fixes_llvm-1.patch</userinput></screen>
-->

<!--
    <note>
      <para>
        <command>./x.py</command> kommandoer kan sende ut en advarselsmelding 
        som klager på <quote><computeroutput>no codegen-backends
        config matched the requested path to build a codegen
        backend</computeroutput></quote>. Og det som er gitt
        <quote>forslag</quote> (<computeroutput>legg til backend til
        codegen-backends i bootstrap.toml</computeroutput>) vil ikke dempe den.
        Denne advarselen er <ulink
        url='https://github.com/rust-lang/rust/issues/110692'>bogus</ulink>
        og det bør ignoreres.
      </para>
    </note>
-->

    <para>
      Kompiler Rust ved å kjøre følgende 
      kommandoer:
    </para>

<!-- fixed in 1.58.0, retain as a reminder that fixed crates can be used
<screen><userinput>sed -i -e '/^curl /s/0.4.38/0.4.40/' \
       -e '/^curl-sys /s/0.4.48/0.4.50/' \
       src/tools/cargo/Cargo.toml &amp;&amp; -->

<screen><userinput>./x.py build</userinput></screen>

    <para>
      Nå, som &root; bruker, installer pakken:
    </para>

<screen role='root'><userinput>./x.py install</userinput></screen>

    <para>
      Fortsatt som &root; bruker, fiks installasjonen av dokumentasjonen og 
      symbolkoble Zsh ferdigstillelsesfilen til 
      riktig sted og flytt en Bash fullføringsfilen 
      til stedet anbefalt av Bash
      fullføringsvedlikeholdere:
    </para>

<screen role='root'><userinput>rm -fv /opt/rustc-&rustc-version;/share/doc/rustc-&rustc-version;/*.old   &amp;&amp;
&install-file; README.md \
  -t /opt/rustc-&rustc-version;/share/doc/rustc-&rustc-version;/ &amp;&amp;
&install-dir; /usr/share/zsh/site-functions &amp;&amp;
ln -sfv /opt/rustc/share/zsh/site-functions/_cargo \
        /usr/share/zsh/site-functions &amp;&amp;

mv -v /etc/bash_completion.d/cargo \
      /usr/share/bash-completion/completions</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    <para>
      <command>ln -svfn rustc-&rustc-version; /opt/rustc</command>: Hvis dette 
      ikke er førstegangs bruk av <filename class="directory">/opt/rustc</filename>
      sybbollenke, overskriv den ved å tvinge den frem, og bruk '-n'-flagget for 
      å unngå å få forvirrende resultater fra f.eks. <command>ls -l</command>.
    </para>

    <para>
      <literal>targets = [...]</literal>: Dette bygger målene som vil være 
      nødvendige for 32-bits Rust prosjekter.
    </para>

    <para>
      <literal>tools = ["cargo", "clippy", "rustdoc", "rustfmt"]</literal>:
      Bygg bare verktøyene fra standardprofilen i den binære kommandoen
      <command>rustup</command> som anbefales for de fleste brukere. De andre 
      verktøyene vil sannsynligvis ikke være nyttige med mindre man bruker 
      (gamle) kodeanalysatorer eller redigerer standardbiblioteket.
    </para>

    <para>
      <literal>channel = "stable"</literal>: Dette sikrer at bare stabile funksjoner 
      kan brukes, standardinnstillingen i <filename>bootstrap.toml</filename> 
      er å bruke utviklingsfunksjoner, noe som ikke er passende for en utgitt versjon.
    </para>

    <!-- comment while using shipped LLVM -->
    <para>
      <literal>[target.x86_64-unknown-linux-gnu]</literal>: Syntaksen til
      <filename>bootstrap.toml</filename> krever en
      <literal>llvm-config</literal> oppføring for hvert mål som system-LLVM 
      skal brukes til. Endre målet til
      <literal>[target.i686-unknown-linux-gnu]</literal> hvis du bygger på 
      32-bit x86. Hele denne delen kan utelates hvis du ønsker å bygge mot den 
      leverte LLVM-en, eller ikke ha clang, men den resulterende byggingen vil 
      være større og ta lengre tid.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Konfigurere Rustc</title>

    <sect3 id="rustc-config">
      <title>Konfigurasjonsinformasjon</title>

      <para>
        Hvis du installerte Rustc i
        <filename class="directory">/opt</filename>, du må oppdatere følgende 
        konfigurasjonsfiler slik at Rustc
        blir funnet riktig av andre pakker og systemprosesser.
      </para>

      <note>
        <para>
          Følgende kommando avhenger av filene som er opprettet i
          <xref linkend="bash-profile"/>.
        </para>
      </note>

      <para>
        Opprett <filename>/etc/profile.d/rustc.sh</filename> oppstartsfil som
        &root; bruker:
      </para>

<screen role="root"><userinput>cat &gt; /etc/profile.d/rustc.sh &lt;&lt; "EOF"
<literal># Begin /etc/profile.d/rustc.sh

pathprepend /opt/rustc/bin           PATH

# End /etc/profile.d/rustc.sh</literal>
EOF</userinput></screen>

    &source-profile;

    </sect3>
  </sect2>


  <sect2 role="content">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installerte Programmer</segtitle>
      <segtitle>Installerte Biblioteker</segtitle>
      <segtitle>Installerte Mapper</segtitle>

      <seglistitem>
        <seg>
          cargo-clippy, cargo-fmt, cargo, clippy-driver, rust-gdb,
          rust-gdbgui, rust-lldb, rustc, rustdoc, og rustfmt
        </seg>
        <seg>
          librustc-driver-&lt;16-byte-hash&gt;,
          libstd-&lt;16-byte-hash&gt;, og
          libtest-&lt;16-byte-hash&gt;
        </seg>
        <seg>
          ~/.cargo,
          /opt/rustc, symbolsk lenke til
          /opt/rustc-&rustc-version;
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="cargo-clippy">
        <term><command>cargo-clippy</command></term>
        <listitem>
          <para>
            tilbyr kodeanalyse for en cargo pakke
          </para>
          <indexterm zone="rustc cargo-clippy">
            <primary sortas="b-cargo-clippy">cargo-clippy</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="cargo-fmt">
        <term><command>cargo-fmt</command></term>
        <listitem>
          <para>
            formaterer alle bin og lib filer i gjeldende crate ved hjelp av
            rustfmt
          </para>
          <indexterm zone="rustc cargo-fmt">
            <primary sortas="b-cargo-fmt">cargo-fmt</primary>
          </indexterm>
        </listitem>
      </varlistentry>

<!--  <varlistentry id="cargo-miri">
        <term><command>cargo-miri</command></term>
        <listitem>
          <para>
            er til bruk for Miri for å tolke bin crates og tester. 
            Det er ikke installert som standard.
          </para>
          <indexterm zone="rust cargo-miri">
            <primary sortas="b-cargo-miri">cargo-miri</primary>
          </indexterm>
        </listitem>
      </varlistentry>-->

      <varlistentry id="cargo">
        <term><command>cargo</command></term>
        <listitem>
          <para>
            er pakkebehandleren for Rust
          </para>
          <indexterm zone="rustc cargo">
            <primary sortas="b-cargo">cargo</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="clippy-driver">
        <term><command>clippy-driver</command></term>
        <listitem>
          <para>
            gir kodeanalyse for rust
          </para>
          <indexterm zone="rustc clippy-driver">
            <primary sortas="b-clippy-driver">clippy-driver</primary>
          </indexterm>
        </listitem>
      </varlistentry>

<!--  <varlistentry id="miri">
        <term><command>miri</command></term>
        <listitem>
          <para>
            er en tolk for Rusts mellomliggende representasjon (MIR). 
            Den er ikke installert som standard.
          </para>
          <indexterm zone="rust miri">
            <primary sortas="b-miri">miri</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rls">
        <term><command>rls</command></term>
        <listitem>
          <para>
            er rust språkserveren. Denne kan kjøre i bakgrunnen for å gi 
            IDE-er, redigeringsprogrammer og andre verktøy informasjon om Rust 
            programmer
          </para>
          <indexterm zone="rust rls">
            <primary sortas="b-rls">rls</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-analyzer">
        <term><command>rust-analyzer</command></term>
        <listitem>
          <para>
            er en implementering av Language Server Protocol for 
            programmeringsspråket Rust.
          </para>
          <indexterm zone="rust rust-analyzer">
            <primary sortas="b-rust-analyzer">rust-analyzer</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-demangler">
        <term><command>rust-demangler</command></term>
        <listitem>
          <para>
            konverterer en liste med rust mangled symboler til en 
            tilsvarende liste med demangled symboler
          </para>
          <indexterm zone="rust rust-demangler">
            <primary sortas="b-rust-demangler">rust-demangler</primary>
          </indexterm>
        </listitem>
      </varlistentry> -->

      <varlistentry id="rust-gdb">
        <term><command>rust-gdb</command></term>
        <listitem>
          <para>
            er et innpakningsskript for gdb, som trekker inn Python 
            pretty-printing moduler som er installert i
            <filename class="directory">/opt/rustc-&rustc-version;/lib/rustlib/etc</filename>
          </para>
          <indexterm zone="rustc rust-gdb">
            <primary sortas="b-rust-gdb">rust-gdb</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-gdbgui">
        <term><command>rust-gdbgui</command></term>
        <listitem>
          <para>
            er et innpakningsskript for en grafisk grensesnitt til gdb som 
            kjører i en nettleser
          </para>
          <indexterm zone="rustc rust-gdbgui">
            <primary sortas="b-rust-gdbgui">rust-gdbgui</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-lldb">
        <term><command>rust-lldb</command></term>
        <listitem>
          <para>
            er et innpakningsskript for LLDB (LLVM-feilsøkingsprogrammet) 
            som henter inn Python pretty-printing modulene
          </para>
          <indexterm zone="rustc rust-lldb">
            <primary sortas="b-rust-lldb">rust-lldb</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustc-bin">
        <term><command>rustc</command></term>
        <listitem>
          <para>
            er Rust kompilatoren
          </para>
          <indexterm zone="rustc rustc-bin">
            <primary sortas="b-rustc-bin">rustc</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustdoc">
        <term><command>rustdoc</command></term>
        <listitem>
          <para>
            genererer dokumentasjon fra Rust kildekoden
          </para>
          <indexterm zone="rustc rustdoc">
            <primary sortas="b-rustdoc">rustdoc</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustfmt">
        <term><command>rustfmt</command></term>
        <listitem>
          <para>
            formaterer Rust kode
          </para>
          <indexterm zone="rustc rustfmt">
            <primary sortas="b-rustfmt">rustfmt</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libstd">
        <term><filename class="libraryfile">libstd-&lt;16-byte-hash&gt;</filename></term>
        <listitem>
          <para>
            er Rust Standardbiblioteket, grunnlaget for flyttbar Rust programvare
          </para>
          <indexterm zone="rustc libstd">
            <primary sortas="c-libstd">libstd-&lt;16-byte-hash&gt;</primary>
          </indexterm>
        </listitem>
      </varlistentry>
    </variablelist>
  </sect2>

</sect1>
