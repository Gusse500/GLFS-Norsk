<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY gbm-download "https://mesa.freedesktop.org/archive/mesa-&mesa-version;.tar.xz">
]>

<sect1 id="gbm" xreflabel="GBM fra Mesa-&mesa-version;">
  <?dbhtml filename="gbm.html"?>

  <title>GBM fra Mesa-&mesa-version;</title>

  <indexterm zone="gbm">
    <primary sortas="a-GBM">GBM</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction to GBM</title>

    <para>
      GBM
        <footnote><para>
          Graphics Buffer Manager.
        </para></footnote>
      biblioteket tilbyr API funksjoner for grafisk gjengivelse og leveres av
      Mesa. De fleste drivere på Linux distribusjoner 
      kommer fra Mesa, så GBM
      vil bli installert sammen med disse driverne. På den annen side, <xref
      linkend="nvidia"/> trenger ikke den fulle Mesa
      stabelen, men den trenger fortsatt GBM biblioteket. 
      Som sådan kan du installere akkurat det du trenger. Du trenger ikke å 
      installere hele <xref linkend="mesa"/> stabelen hvis du vil installere <xref
      linkend="nvidia"/>, men du kan hvis du ønsker det. Det kan være lurt å 
      gjøre det for å ha et reserveprogram (Zink Gallium3D + Nouveau Vulkan). 
      Hvis du ikke installerer NVIDIA, hopp til <xref
      linkend="llvm"/> og fortsett til du kommer frem til <xref
      linkend="mesa"/> for å få en fullstendig installasjon av drivere og 
      andre biblioteker.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Nedlasting: <ulink url="&gbm-download;"/>
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">GBM Avhengigheter</bridgehead>

    <bridgehead renderas="sect4">Påkrevd</bridgehead>
    <para role="required">
      <xref linkend="libdrm"/>,
      <xref linkend="libxcb"/>,
      <xref linkend="mako"/>,
      <xref linkend="pyyaml"/>, og
      <xref linkend="xorg7-lib"/>
    </para>

    <bridgehead renderas="sect4">Valgfri</bridgehead>
    <para role="optional">
      <ulink url="&slfs-website;/general/mesa-demos.html">Mesa-Demos</ulink>
      (tilbyr mer enn 300 demonstrasjoner for å teste drivere)
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av GBM</title>
    
    <para>
      Installer GBM ved å kjøre følgende 
      kommandoer:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr            \
            --buildtype=release      \
            -D gallium-drivers=      \
            -D vulkan-drivers=       \
            -D video-codecs=         \
            -D gbm=enabled           \
            -D glx=disabled          \
            -D llvm=disabled         \
            -D valgrind=disabled     \
            -D libunwind=disabled    \
            .. &amp;&amp;

ninja</userinput></screen>

    <para>
      Nå, som &root; bruker:
    </para>

<screen role="root"><userinput>ninja install &amp;&amp;
mkdir -pv /usr/include/GL/internal &amp;&amp;
cp -v ../include/GL/internal/dri_interface.h /usr/include/GL/internal
if test -d /usr/include/EGL; then
  cp -v ../include/EGL/eglmesaext.h /usr/include/EGL
fi</userinput></screen>

  </sect2>

  <sect2 role="installation">
    <title>lib32 Installasjon av GBM</title>

    <para>
      Installer lib32-GBM ved å kjøre følgende 
      kommandoer:
    </para>

<screen><userinput>rm -rf * &amp;&amp;
CC="gcc -m32" CXX="g++ -m32"         \
PKG_CONFIG_PATH=/usr/lib32/pkgconfig \
meson setup --prefix=/usr            \
            --libdir=/usr/lib32      \
            --buildtype=release      \
            -D gallium-drivers=      \
            -D vulkan-drivers=       \
            -D video-codecs=         \
            -D gbm=enabled           \
            -D glx=disabled          \
            -D llvm=disabled         \
            -D valgrind=disabled     \
            -D libunwind=disabled    \
            .. &amp;&amp;

ninja</userinput></screen>

    <para>
      Nå, som <systemitem class="username">root</systemitem> bruker:
    </para>

<screen role="root"><userinput>DESTDIR=$PWD/DESTDIR ninja install    &amp;&amp;
cp -vr DESTDIR/usr/lib32/* /usr/lib32 &amp;&amp;
rm -rf DESTDIR                        &amp;&amp;
ldconfig</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-meson;

    <para>
      <parameter>--buildtype=release</parameter>: Denne bryteren sikrer en 
      fullstendig optimalisert bygging, og deaktiverer feilsøkingspåstander som 
      vil gjøre bibliotekene betydelig tregere i visse brukstilfeller. Uten 
      denne bryteren kan byggestørrelser strekke seg inn i 2 GB området.
    </para>

    <para>
      <parameter>-D gallium-drivers=</parameter>: Denne parameteren tvinger 
      Gallium3D (OpenGL) drivere til å ikke bygges, ettersom drivere leveres av
      <xref linkend="nvidia"/>.
    </para>

    <para>
      <parameter>-D vulkan-drivers=</parameter>: Denne parameteren tvinger
      Vulkan drivere til å ikke bygges, ettersom drivere leveres av <xref
      linkend="nvidia"/>.
    </para>

    <para>
      <parameter>-D video-codecs=</parameter>: Denne parameteren tvinger video
      kodeker å ikke bygges.
    </para>

    <para>
      <parameter>-D gbm=enabled</parameter>: Denne parameteren tvinger <filename
      class="libraryfile">libgbm</filename> til å bygges.
    </para>

    <para>
      <parameter>-D glx=disabled</parameter>: Denne parameteren deaktiverer 
      bygging av OpenGL biblioteker eller drivere som leveres av <xref
      linkend="nvidia"/>.
    </para>

    <para>
      <parameter>-D llvm=disabled</parameter>: Denne parameteren deaktiverer 
      unødvendig <xref linkend="llvm"/> støtte.
    </para>

    <para>
      <parameter>-D valgrind=disabled</parameter>: Denne parameteren deaktiverer
      bruken av Valgrind under byggeprosessen.
    </para>

    <para>
      <parameter>-D libunwind=disabled</parameter>: Denne parameteren deaktiverer
      bruken av libunwind.
    </para>

    <para>
      <command>cp -v ../include/GL/internal/dri_interface.h ...</command>: Denne
      kommandoen kopierer over en deklarasjonsfil som trengs av <xref
      linkend="xorg-server"/> siden den ikke automatisk kopieres over i dette
      bygget.
    </para>

    <para>
      <command>cp -v ../include/EGL/eglmesaext.h ...</command>: Denne kommandoen
      kopierer over en deklarasjonsfil som trengs av <ulink
      url="&blfs-svn;/gnome/mutter.html">Mutter</ulink> og muligens andre
      pakker.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installerte Programmer</segtitle>
      <segtitle>Installerte Biblioteker</segtitle>
      <segtitle>Installerte Mapper</segtitle>

      <seglistitem>
        <seg>
          Ingen
        </seg>
        <seg>
          libgbm
        </seg>
        <seg>
          /usr/share/drirc.d (inneholder løsninger for diverse applikasjoner, 
          spesielt nettlesere og spill)
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libgbm-gbm">
        <term><filename class="libraryfile">libgbm</filename></term>
        <listitem>
          <para>
            er Grafikk bufringsbehandler biblioteket
          </para>
          <indexterm zone="gbm libgbm-gbm">
            <primary sortas="c-libgbm-gbm">libgbm</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
