<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY nv-root "https://us.download.nvidia.com/XFree86/Linux-x86_64">
  <!ENTITY nvidia-download
    "&nv-root;/&nvidia-r580-version;/NVIDIA-Linux-x86_64-&nvidia-r580-version;.run">
  <!ENTITY mesa-raw-repo
    "https://gitlab.freedesktop.org/mesa/mesa/-/raw/&mesa-major-minor;">
]>

<sect1 id="nvidia-r580" xreflabel="NVIDIA-&nvidia-r580-version;">
  <?dbhtml filename="nvidia-r580.html"?>

  <title>NVIDIA-&nvidia-r580-full-name;</title>

  <indexterm zone="nvidia-r580">
    <primary sortas="a-nvidia-r580">NVIDIA-r580</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til NVIDIA-r580</title>

    <para>
      Dette er hovedutgivelsen r580 av NVIDIAs proprietære driver, som 
      inneholder fastvare, kjernedrivere, brukerlanddrivere knyttet til OpenGL, 
      Vulkan og maskinvareakselerasjon, og tilbyr nyttige verktøy for NVIDIA 
      kort fra Maxwell til Blackwell.
    </para>

    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Anbefalt(e) kjerneversjon(er) å bruke: linux &nvidia-r580-linux-version;
        </para>
      </listitem>
      <listitem>
        <para>
          Nedlasting: <ulink url="&nvidia-download;"/>
        </para>
      </listitem>
    </itemizedlist>

<!--
    <bridgehead renderas="sect3">Ytterligere Nedlastinger</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Påkrevd oppdatering:
          <ulink url="&dl-root;/NVIDIA/NVIDIA-&nvidia-r580-version;-kernel_gpl_cachyos-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->

    <bridgehead renderas="sect3">NVIDIA-r580 Avhengigheter</bridgehead>

    <para role="required">
      &nvidia-common-req;
    </para>

    <para role='recommended'>
      <itemizedlist spacing='compact'>
        <listitem>
          <para>
            <xref linkend="cuda-r580"/>
            (for å bygge inn direkte CUDA støtte i diverse pakker som FFmpeg 
            eller OBS-Studio; installer etter å ha fulgt denne siden)
          </para>
        </listitem>
        &nvidia-common-rec;
      </itemizedlist>
    </para>

  </sect2>

  <sect2>
    <title>Installasjon av NVIDIA driveren</title>

    <para>
      Installer NVIDIA driveren ved å kjøre følgende kommando som &root;
      bruker:
    </para>

<screen><userinput>sh NVIDIA-Linux-x86_64-&nvidia-version;.run &nvidia-runfile-common;</userinput></screen>

    &nvidia-runfile-explanation;

  </sect2>

  <sect2 role="configuration">
    <title>Konfigurere NVIDIA-r580</title>

    <sect3 id="nvidia-config-r580">
      <title>Konfigurasjonsfiler</title>
      <para>
        <filename>/usr/lib/modprobe.d/nouveau.conf</filename>
      </para>
      <para>
        <filename>/usr/lib/modprobe.d/nvidia_drm.conf</filename>
      </para>
      <para revision="systemd">
        <filename>/usr/lib/modprobe.d/nvidia.conf</filename>
      </para>
    </sect3>

    &nvidia-issue-list;

    <sect3><title>Konfigurasjonsinformasjon</title>

      <para>
        For at driverne skal fungere ordentlig, må enhetsnoder opprettes på 
        forhånd. Under visse forhold eller konfigurasjoner opprettes ikke disse 
        automatisk når en skjermserver startes. Som &root; bruker, opprett en 
        udev regel for alltid å opprette disse nodene ved oppstart:
      </para>

<screen role="root"><userinput>mkdir -pv /usr/lib/udev/rules.d &amp;&amp;
cat &gt; /usr/lib/udev/rules.d/60-nvidia.rules &lt;&lt; "EOF"
<literal># Always create NVIDIA device nodes at boot time.
ACTION=="add|bind",                \
  ATTR{vendor}=="0x10de",          \
  ATTR{class}=="0x03[0-9]*",       \
  DRIVER=="nvidia",                \
  TEST!="/dev/nvidiactl",          \
  RUN+="/usr/bin/nvidia-modprobe", \
  RUN+="/usr/bin/nvidia-modprobe -c0 -u"</literal>
EOF</userinput></screen>

      <para>
        Nouveau og NVIDIAFB støtte ble kompilert inn i kjernen for å sikre at 
        NVIDIA kjernemodulene kunne bygges. Nouveau og NVIDIAFB er imidlertid i 
        konflikt med NVIDIA kjernemodulene, så de bør svartelistes. I tillegg 
        kan det ha blitt bygget andre moduler som kan komme i konflikt, og bør 
        også deaktiveres. Som &root; bruker, opprett filen
        <filename>/usr/lib/modprobe.d/nouveau.conf</filename> for å svarteliste 
        Nouveau og NVIDIAFB, samt andre motstridende moduler:
      </para>

<screen role="root"><userinput>mkdir -pv /usr/lib/modprobe.d &amp;&amp;
cat &gt; /usr/lib/modprobe.d/nouveau.conf &lt;&lt; "EOF"
<literal># Begin /usr/lib/modprobe.d/nouveau.conf

blacklist nouveau
blacklist nvidiafb
blacklist nova_core
blacklist nova_drm

# End /usr/lib/modprobe.d/nouveau.conf</literal>
EOF</userinput></screen>

      <para>
        DRM kjernemodulen bruker ikke modusinnstilling som standard, noe som er 
        nødvendig for Wayland kompositorer. Gjør det mulig å bruke 
        modusinnstilling som &root; bruker:
      </para>

<screen role="root"><userinput>cat &gt; /usr/lib/modprobe.d/nvidia_drm.conf &lt;&lt; "EOF"
<literal># Begin /usr/lib/modprobe.d/nvidia_drm.conf

options nvidia_drm modeset=1

# End /usr/lib/modprobe.d/nvidia_drm.conf</literal>
EOF</userinput></screen>

      <!-- Cannot be used on SysVinit and the kernel module will complain
           as the ability to make use of sleep/hibernate is not yet
           implemented. We must provide the files, but they do not work.
           - Zeckma -->
      <para revision="systemd">
        Når GPU-en går i hvilemodus/dvalemodus, mister den som standard alle 
        minnetildelinger. Du kan forhindre dette ved å angi to alternativer for 
        NVIDIA modulen. Hvis du ikke bruker hvilemodus/dvalemodus, kan du hoppe 
        over å lage følgende konfigurasjonsfil. Ellers, som &root; bruker:
      </para>

<screen revision="systemd" role="root"><userinput>cat &gt; /usr/lib/modprobe.d/nvidia.conf &lt;&lt; "EOF"
<literal># Begin /usr/lib/modprobe.d/nvidia.conf

options nvidia NVreg_PreserveVideoMemoryAllocations=1
options nvidia NVreg_TemporaryFilePath=/var/tmp

# End /usr/lib/modprobe.d/nvidia.conf</literal>
EOF</userinput></screen>

    </sect3>

  </sect2>

  <sect2>
    <title>Fortsette</title>

    <para>
      Hvis du installerte <xref linkend="libglvnd"/> og <xref linkend="libgbm"/>,
      burde du ikke trenge Mesa som en avhengighet for noen pakke i noen av LFS 
      bøkene. Du må sørge for at du installerer <xref linkend="xorg-server"/>
      og/eller <xref linkend="xwayland"/> fra denne boken, ettersom disse pakkene 
      inkluderer viktige oppdateringer som omgår behovet for et Mesa spesifikt 
      grensesnitt som brukes til å laste inn Mesa spesifikke drivere for programvaregjengivelse.
    </para>

    <para>
      Med dette Mesa frie oppsettet, kan du hoppe til <xref linkend="x"/> og
      forvent problemfri bruk. Hvis du vil, kan du imidlertid også installere 
      CUDA i denne underseksjonen for å forbedre ytelsen, noe som er opp til deg.
    </para>

  </sect2>

</sect1>
