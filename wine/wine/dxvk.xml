<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="dxvk" xreflabel="DXVK-&dxvk-version;">
  <?dbhtml filename="dxvk.html"?>

  <title>DXVK-&dxvk-version;</title>

  <indexterm zone="dxvk">
    <primary sortas="a-dxvk">DXVK</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduksjon til DXVK</title>

    <para>
      DXVK pakken inneholder DLL filer som 
      konverterer Direct3D 8-11 kall til Vulkan. Som standard i
      Wine, Direct3D 8-11 kall konverteres til 
      OpenGL kall på en dårlig måte, noe som fører til dårlig ytelse. Derfor 
      installerer mange denne pakken.
    </para>

    &long-build-time;

    <bridgehead renderas="sect3">DXVK Avhengigheter</bridgehead>

    <para role="required">
      <xref linkend="mingw-w64-gcc"/> (kompilert med POSIX trådstøtte),
      <xref linkend="vulkan-sdk"/>, og
      <xref linkend="wine"/>
    </para>

  </sect2>

  <sect2>
    <title>Laste ned DXVK</title>

    <para>
      Denne pakken er avhengig av undermoduler, og flere av dem må lastes ned. 
      Lag en liste over nødvendige tar filer som skal lastes ned:
    </para>

<screen><userinput>cat &gt; dxvk-&dxvk-version;-list &lt;&lt; "EOF"
<literal>doitsujin/dxvk/archive/v&dxvk-version;/dxvk-&dxvk-version;.tar.gz
doitsujin/libdisplay-info/archive/&dxvk-libdisplay-info;.tar.gz
KhronosGroup/SPIRV-Headers/archive/&dxvk-spirv;.tar.gz
KhronosGroup/Vulkan-Headers/archive/&dxvk-vulkan;.tar.gz</literal>
EOF</userinput></screen>

    <para>
      Last ned tarballene ved å bruke <xref linkend="wget"/>:
    </para>

<screen><userinput>mkdir dxvk &amp;&amp;
cd    dxvk &amp;&amp;
grep -v '^#' ../dxvk-&dxvk-version;-list | wget -i- -c \
    -B https://github.com/</userinput></screen>

  </sect2>

  <sect2 role="installation">
    <title>Installasjon av DXVK</title>

    <para>
      Pakk ut alle tarballene:
    </para>

<screen><userinput>for i in *.tar.?z*; do
  echo "Extracting $i..."
  tar -xf $i
done</userinput></screen>

    <para>
      Nå som tarballene er pakket ut, må undermodulene flyttes på plass. 
      Gjør det nå:
    </para>

<screen><userinput>mv -T libdisplay-info-*                       \
      dxvk-&dxvk-version;/subprojects/libdisplay-info &amp;&amp;
mv -T SPIRV-Headers-*                         \
      dxvk-&dxvk-version;/include/spirv               &amp;&amp;
mv -T Vulkan-Headers-*                        \
      dxvk-&dxvk-version;/include/vulkan</userinput></screen>

    <para>
      Gå inn i <filename class="directory">dxvk-&dxvk-version;</filename>
      mappen:
    </para>

<screen><userinput>cd dxvk-&dxvk-version;</userinput></screen>

    <note><para>
      Hvis du bruker multilib, anbefales det å installere 32-bit sammen med
      64-bit DXVK siden det finnes mange 32-biters 
      Direct3D applikasjoner.
    </para></note>

    <sect3>
      <title>64-bit Installasjon</title>

      <para>
        Installer 64-bit DXVK ved å kjøre 
        følgende kommandoer:
      </para>

<screen><userinput>mkdir build-win64 &amp;&amp;
cd    build-win64 &amp;&amp;

meson setup --cross-file=../build-win64.txt \
            --buildtype=release             \
            --prefix=$PWD/DESTDIR           \
            .. &amp;&amp;
ninja</userinput></screen>

      <para>Nå som &root; bruker:</para>

<screen role="root"><userinput>ninja install &amp;&amp;
cd ..         &amp;&amp;

mkdir   -pv     /usr/lib/dxvk/win64 &amp;&amp;
&install-exec; build-win64/DESTDIR/bin/*.dll \
             -t /usr/lib/dxvk/win64/</userinput></screen>

    </sect3>

    <sect3>
      <title>32-bit Installasjon</title>

      <para>
        Installer 32-bit DXVK ved å kjøre 
        følgende kommandoer:
      </para>

<screen><userinput>mkdir build-win32 &amp;&amp;
cd    build-win32 &amp;&amp;

meson setup --cross-file=../build-win32.txt \
            --buildtype=release             \
            --prefix=$PWD/DESTDIR           \
            .. &amp;&amp;
ninja</userinput></screen>

      <para>Nå som &root; bruker:</para>

<screen role="root"><userinput>ninja install &amp;&amp;
cd ..         &amp;&amp;

mkdir   -pv     /usr/lib/dxvk/win32 &amp;&amp;
&install-exec; build-win32/DESTDIR/bin/*.dll \
             -t /usr/lib/dxvk/win32/</userinput></screen>

    </sect3>

  </sect2>

  <sect2 role="configuration">
    <title>Sette opp DXVK</title>

    <note><para>
      Denne delen krever et kjørende grafisk miljø. Hvis du ikke er 
      klar ennå, kan du hoppe over denne delen og gå tilbake når du 
      er i et grafisk miljø.
    </para></note>

    <para>
      Først, opprett Wine prefiks hvis det ikke 
      allerede er opprettet:
    </para>

    <screen><userinput>WINEPREFIX=~/.wine wineboot -u</userinput></screen>

    <note><para>
      <command>wineboot</command> kan rapportere en feil i utdataene, men dette 
      er normalt og hyppig. Vanligvis er disse feilene ikke fatale, og du trenger 
      vanligvis ikke å bekymre deg for dem. Wine er 
      veldig detaljert.
    </para></note>

    <para>Selv om DXVK DLL filene er installert, er de ennå ikke i
    Wine prefiks. Lag symbolske lenker slik at de kan 
    finnes av Wine, samtidig som det er mulig å 
    oppdatere denne pakken uten å måtte kopiere alt på nytt. I tillegg til dette
    må registeret oppdateres for å bruke de nye DLL-ene. Gjør dette for hver 
    arkitektur du har installert.</para>

    <sect3>
      <title>64-bit Oppsett</title>

      <para>For 64-bit:</para>

<screen><userinput>for DLL_FILE in /usr/lib/dxvk/win64/*.dll; do
    ln -sfv $DLL_FILE ~/.wine/drive_c/windows/system32/$(basename $DLL_FILE) &amp;&amp;
    DLL_FILE=$(basename $DLL_FILE | sed s/.dll//g) &amp;&amp;
    wine reg add \
        'HKEY_CURRENT_USER\Software\Wine\DllOverrides' \
        /v "$DLL_FILE" /d native /f
done</userinput></screen>

    </sect3>

    <sect3>
      <title>32-bit Oppsett</title>

      <para>For 32-bit:</para>

<screen><userinput>for DLL_FILE in /usr/lib/dxvk/win32/*.dll; do
    if [ ! -d ~/.wine/drive_c/windows/syswow64 ]; then
        ln -sfv $DLL_FILE ~/.wine/drive_c/windows/system32/$(basename $DLL_FILE)
    else
        ln -sfv $DLL_FILE ~/.wine/drive_c/windows/syswow64/$(basename $DLL_FILE)
    fi
    DLL_FILE=$(basename $DLL_FILE | sed s/.dll//g) &amp;&amp;
    wine reg add \
        'HKEY_CURRENT_USER\Software\Wine\DllOverrides' \
        /v "$DLL_FILE" /d native /f
done</userinput></screen>

    </sect3>

  </sect2>

  <sect2 role="commands">
    <title>Parameterforklaringer</title>

    &options-meson;

    <para>
      <parameter>--cross-file=../build-win{32,64}.txt</parameter>: Denne 
      parameteren endrer hvilken Win arkitektur det skal bygges for.
    </para>

    &meson-buildtype-release;

    <para>
      <command>WINEPREFIX=~/.wine wineboot -u</command>: Denne kommandoen 
      oppretter en Wine prefiks og kopierer 
      eventuelle manglende filer til det. Det vil ikke tilbakestille et 
      eksisterende prefiks.
    </para>

    <para>
      <command>wine reg add ...</command>: Denne kommandoen overstyrer 
      registeret slik at Wine vil bruke de nye DLL 
      filene i stedet for de gamle.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Innhold</title>

    <segmentedlist>
      <segtitle>Installerte Programmer</segtitle>
      <segtitle>Installerte Biblioteker</segtitle>
      <segtitle>Installerte Mapper</segtitle>

      <seglistitem>
        <seg>
          Ingen
        </seg>
        <seg>
          d3d10core,
          d3d11,
          d3d8,
          d3d9, and
          dxgi (alle DLL-er)
        </seg>
        <seg>
          /usr/lib/dxvk/{win32,win64}
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Korte Beskrivelser</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="d3d10core">
        <term><filename class="libraryfile">d3d10core</filename></term>
        <listitem>
          <para>
            konverterer Direct3D 10 kall til Vulkan
          </para>
          <indexterm zone="dxvk d3d10core">
            <primary sortas="c-d3d10core">d3d10core</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="d3d11">
        <term><filename class="libraryfile">d3d11</filename></term>
        <listitem>
          <para>
            konverterer Direct3D 11 kall til Vulkan
          </para>
          <indexterm zone="dxvk d3d11">
            <primary sortas="c-d3d11">d3d11</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="d3d8">
        <term><filename class="libraryfile">d3d8</filename></term>
        <listitem>
          <para>
            konverterer Direct3D 8 kall til Vulkan
          </para>
          <indexterm zone="dxvk d3d8">
            <primary sortas="c-d3d8">d3d8</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="d3d9">
        <term><filename class="libraryfile">d3d9</filename></term>
        <listitem>
          <para>
            konverterer Direct3D 9 kall til Vulkan
          </para>
          <indexterm zone="dxvk d3d9">
            <primary sortas="c-d3d9">d3d9</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dxgi">
        <term><filename class="libraryfile">dxgi</filename></term>
        <listitem>
          <para>
            håndterer Direct3D grafikkinfrastrukturen
          </para>
          <indexterm zone="dxvk dxgi">
            <primary sortas="c-dxgi">dxgi</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
